executor mimir skill forge bc replay-input creation from opaque caller-admitted replay bytes realization next step
repo root: D:\RocketLeague bot\MIMIR
date: 2026-04-29

mimir_export boundary
- `mimir_export` widening remains forbidden unless explicitly reopened
- no export bundle, sidecar, manifest, index, runtime export, or downstream consumer semantics may be silently widened from this realization pass

next pass type
- narrow post-realization boundary decision pass for the low-boost-recovery ReplayInput-creation realization result

exact next input
- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputCreationFromOpaqueCallerAdmittedReplayBytesRealizationResultV1`

exact next responsibility
- decide whether to stop at the realized memory ReplayInput bridge or explicitly reopen exactly one downstream domain
- preserve the fact that ReplayInput memory creation is not replay parsing, parser success, carrier discovery, locator logic, source materialization, raw-state parsing, or export widening
- if a downstream domain is selected, define only the next narrow contract boundary and do not implement it in the decision pass

still forbidden unless explicitly reopened
- replay-source actual-materialization implementation
- replay-source carrier discovery
- replay-input locator logic
- actual replay parsing
- parser-success logic
- raw-state payload parsing
- tensor/control materialization
- corpus-wide replay ingestion
- real rollout physics
- async/background systems
- database code
- runtime CLI commands
- execution-result cleanup boundary changes
- `mimir_export` widening
- `mimir-replay` source changes
- `mimir-io` changes
- `mimir-export` changes
- `mimir-types` changes
- generic all-family replay/raw-state/index/export/materialization framework creation

reason this is the next step
- this pass realized only the contract-defined memory ReplayInput bridge
- the next missing decision is not implementation; it is choosing whether any downstream domain should be reopened at all
