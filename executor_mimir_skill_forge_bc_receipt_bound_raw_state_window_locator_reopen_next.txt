executor mimir skill forge bc receipt-bound raw-state-window locator reopen next step
repo root: D:\RocketLeague bot\MIMIR
date: 2026-04-15

next pass
- a first narrow contract-definition pass for
  `LowBoostRecoveryBcReceiptBoundSpecimenFileAnchoredRawStateWindowLocatorContractV1`
- `mimir_export` widening remains forbidden unless explicitly reopened

why this is the immediate next pass
- This pass already made the reopen decision explicit.
- This pass already fixed the narrowest honest contract shape.
- Actual raw-state lookup realization is still deferred.
- The next unresolved problem is now smaller:
  - formalize the specimen-file-anchored locator contract itself
  - keep it receipt-bound, family-specific, and contract-only

what the next pass should consume
- `LowBoostRecoveryBcRawStateWindowLookupObservationAccessPlanningResultV1`
- `LowBoostRecoveryBcRawStateWindowLookupRealizationProofResultV1`
- `LowBoostRecoveryBcReceiptBoundRawStateWindowLocatorReopenDecisionResultV1`
- the same audited family root directory reference preserved by the planning result

what the next pass should do
- remain strictly low-boost-recovery-specific
- remain strictly receipt-bound
- define the contract surface for one deterministic BC specimen-file lookup source per admitted
  planning specimen
- preserve the invariant that the audited family root is only a BC specimen-tree anchor
- preserve the invariant that `source_raw_state_window_ref` stays opaque until actual lookup is
  separately reopened
- avoid sidecars, manifests, generic indexing, tensor materialization, control extraction, replay
  parsing, and `mimir_export`

what the next pass must still not do
- `mimir_export` widening remains forbidden unless explicitly reopened
- no sidecar/manifest realization unless a separate defect-driven reopen proves one is the minimum
  honest fix
- no generic multi-family downstream/export framework
- no generic manifest/index framework
- no actual raw-state lookup realization
- no tensor materialization
- no control/action extraction
- no replay parsing/mining/rollout/physics work

classification of the next pass
- a first narrow contract-definition pass for the locator contract

why the next pass is not actual lookup realization
- The reopened contract has only been justified, not defined.
- No actual locator implementation exists yet.
- No raw-state materialization contract exists yet.
- Jumping to realization next would still bypass the proof boundary that required the locator
  contract first.
