pass type: MIMIR Skill Forge v1 low-boost-recovery consumer / BC-planning boundary pass
runtime behavior changed: no
public APIs added: yes
public APIs removed: no
consumer-bc-planning artifact added: yes
BC-milestone-flag artifact added: yes
decision artifact added: yes
next-step artifact added: yes
source/docs changed: yes
mimir_export test count from cargo test -p mimir-export -- --list: 173 tests, 0 benchmarks
exact validation commands run:
- cargo fmt --all
- cargo check --workspace --all-targets --all-features
- cargo test --workspace --all-targets --all-features
- cargo clippy --workspace --all-targets --all-features -- -D warnings
- cargo test -p mimir-export -- --list

validation note:
- One earlier `cargo test --workspace --all-targets --all-features` run failed on a new planning
  test expectation.
- The expectation was corrected to preserve accepted-reference lineage even for unusable eval input
  while still marking the pair `not_bc_candidate`.
- After that correction, the exact required validation sequence above was rerun and passed.
