helper names added or updated:
- None added.
- Reused existing helper: `persisted_input_artifact_for_lane_with_proposal_score_signals`

crate(s) touched:
- `crates/mimir-cli`

which Stage77 proposal index/label was mutated:
- lane: `VerticalSliceFixtureLane::Stage77Second`
- proposal index: `1`
- proposal label: `candidate-delta`

exact Stage77 `score_signals` mutation applied:
- baseline persisted `score_signals`: `{"coverage": 2.0, "stability": 1.0}`
- mutated persisted `score_signals`: `{"coverage": 6.0, "stability": 1.0}`
- only `coverage` changed
- unchanged on both runs: `replay_id`, `export_name`, `teacher_namespace`, `simulation_seed`, `anchor_hint`, proposal labels, proposal actions, proposal `legal_hint`, proposal metadata, `scorer_weights`

exact affected component values before/after:
- affected scoreboard row: `anchor-stage77-second:branch:1`
- component map before: `{"coverage": 2.0, "stability": 0.5}`
- component map after: `{"coverage": 6.0, "stability": 0.5}`
- unaffected scoreboard row stayed: `anchor-stage77-second:branch:0 -> {"coverage": 5.0, "stability": 1.0}`
- component keys stayed identical on both runs for both rows: `["coverage", "stability"]`

exact affected total score before/after:
- `anchor-stage77-second:branch:1` total: `2.5 -> 6.5`
- `anchor-stage77-second:branch:0` total: `6.0 -> 6.0`

whether teacher ordering changed and how:
- yes
- baseline teacher targets in order: `["anchor-stage77-second:branch:0", "anchor-stage77-second:branch:1"]`
- mutated teacher targets in order: `["anchor-stage77-second:branch:1", "anchor-stage77-second:branch:0"]`
- baseline teacher artifact ids in file order: `["stage77-second-vertical-slice:teacher:0", "stage77-second-vertical-slice:teacher:1"]`
- mutated teacher artifact ids in file order: `["stage77-second-vertical-slice:teacher:1", "stage77-second-vertical-slice:teacher:0"]`
- ordering flipped because branch `1` rose above branch `0` while branch `0` stayed at `6.0`

what unrelated surfaces remained stable:
- canonical reload succeeded for baseline and mutated runs
- branch ids stayed `["anchor-stage77-second:branch:0", "anchor-stage77-second:branch:1"]`
- exported anchor payload stayed identical between runs
- proposal actions, `legal_hint`, and metadata stayed identical between runs for both branches
- teacher artifact surfaces unrelated to score consequences stayed stable per branch target:
  - `payload.id`
  - `payload.label`
  - `payload.metadata.ordinal`
  - `payload.metadata.teacher_namespace`
  - `payload.metadata.teacher_namespace_copy`
  - `header.metadata.teacher_namespace`
- frozen snapshot identity/path surfaces stayed stable:
  - `export_name`
  - `replay_id`
  - `anchor_id`
  - `anchor_frame_index`
  - `anchor_kind`
  - export directory name
  - scoreboard artifact relative path
  - teacher artifact relative paths
  - branch ids in order
- frozen snapshot score consequences changed only where expected:
  - scoreboard totals changed for branch `1`
  - teacher target ordering flipped exactly to the new score order

tests added or updated:
- Added `second_deterministic_vertical_slice_score_signals_directly_change_component_values_total_score_and_teacher_ordering_without_perturbing_other_contract_edges`

resulting mimir_export test count from cargo test -p mimir-export -- --list:
- `173 tests, 0 benchmarks`

short note on why this closes the top remaining Stage77 score observability follow-up edge without widening runtime semantics:
- This pass gives `Stage77Second` the missing value-level proof for `PersistedVerticalSliceProposal.score_signals -> scoreboard component values -> total score -> teacher ordering`. It reuses the existing deterministic fake-backend path, preserves component-key identity and unrelated contract surfaces, and adds no new runtime behavior, export semantics, loader semantics, or public API.
