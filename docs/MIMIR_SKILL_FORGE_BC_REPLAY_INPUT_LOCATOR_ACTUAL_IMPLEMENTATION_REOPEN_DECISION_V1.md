# MIMIR Skill Forge BC Replay-Input Locator Actual-Implementation Reopen Decision v1

## A. PURPOSE

### What this pass owns

This pass owns exactly one reopen-decision question above:

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorImplementationRealizationResultV1`

It defines:

- whether actual replay-input locator implementation work must now be deliberately reopened at all
- the exact criteria used to answer that question
- the narrowest honest next contract shape only if reopening is justified

### Why it exists

The boundary below this pass already fixed the current truthful limit:

- `RealizedForTruthfulBlockedReplayInputLocatorImplementationOnly`

That matters because the repo can now prove all of the following:

- one admitted specimen yields one truthful blocked replay-input locator implementation result
- that result preserves receipt-bound specimen identity, deferred observation lineage, and the
  contract-only replay handle kind
- that result still does not implement replay-input location
- that result still does not parse replay input

This pass exists because the repo can now prove it can do no more than a blocked / unavailable
replay-input locator implementation result. The remaining question is whether the next missing work
is now truly actual replay-input locator implementation work.

### How it differs from the replay-input locator implementation realization boundary below it

- The lower boundary realizes the current truth:
  `RealizedForTruthfulBlockedReplayInputLocatorImplementationOnly`.
- This pass does not realize more output.
- This pass decides whether one narrower actual replay-input locator implementation boundary must
  now be deliberately reopened above that truthful blocked result.
- This pass is still not actual replay-input locator implementation.
- This pass is still not replay parsing.

## B. FAMILY SCOPE

`low_boost_recovery` is the only supported family in this reopen-decision version.

This replay-input-locator-actual-implementation reopen decision remains family-specific because:

- the admitted input is only
  `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorImplementationRealizationResultV1`
- the preserved lineage tuple is only the low-boost-recovery BC specimen tuple already carried by
  that result
- the audited family root reference is still only the low-boost-recovery BC specimen-tree anchor
- no second family exists that would justify a shared replay-input locator actual-implementation
  framework

No generic all-family replay/raw-state/index/export/materialization framework is introduced here.

## C. INPUT BOUNDARY

This pass consumes only:

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorImplementationRealizationResultV1`
- the audited family root directory reference already preserved by that result

From
`LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorImplementationRealizationResultV1`,
this pass consumes exactly:

- `specimen_count`
- `group_count`
- `source_contract_disposition`
- `source_contract_notes`
- `source_chosen_locator_contract_shape`
- `source_chosen_materialization_contract_shape`
- `source_chosen_replay_parsing_contract_shape`
- `source_chosen_replay_input_access_contract_shape`
- `source_chosen_replay_input_locator_contract_shape`
- `audited_family_root_directory`
- ordered `preserved_ordered_lane_results`
- each `lane_ordinal`
- each ordered `ordered_specimen_results`
- each `specimen_ordinal`
- each `consumed_replay_input_locator_implementation_boundary_input.lane_ordinal`
- each `consumed_replay_input_locator_implementation_boundary_input.specimen_ordinal`
- each `consumed_replay_input_locator_implementation_boundary_input.artifact_id`
- each `consumed_replay_input_locator_implementation_boundary_input.anchored_bc_specimen_file_path`
- each `consumed_replay_input_locator_implementation_boundary_input.source_raw_state_window_ref`
- each `consumed_replay_input_locator_implementation_boundary_input.source_slice_id`
- each `consumed_replay_input_locator_implementation_boundary_input.source_replay`
- each `consumed_replay_input_locator_implementation_boundary_input.source_subject`
- each `consumed_replay_input_locator_implementation_boundary_input.source_phase_id`
- each `consumed_replay_input_locator_implementation_boundary_input.preserved_observation_binding_kind`
- each `consumed_replay_input_locator_implementation_boundary_input.preserved_accepted_reference_window`
- each
  `preserved_replay_input_locator_implementation_output_boundary.lane_ordinal`
- each
  `preserved_replay_input_locator_implementation_output_boundary.specimen_ordinal`
- each `preserved_replay_input_locator_implementation_output_boundary.artifact_id`
- each
  `preserved_replay_input_locator_implementation_output_boundary.anchored_bc_specimen_file_path`
- each
  `preserved_replay_input_locator_implementation_output_boundary.source_raw_state_window_ref`
- each `preserved_replay_input_locator_implementation_output_boundary.source_slice_id`
- each `preserved_replay_input_locator_implementation_output_boundary.source_replay`
- each `preserved_replay_input_locator_implementation_output_boundary.source_subject`
- each `preserved_replay_input_locator_implementation_output_boundary.source_phase_id`
- each
  `preserved_replay_input_locator_implementation_output_boundary.preserved_observation_binding_kind`
- each
  `preserved_replay_input_locator_implementation_output_boundary.preserved_accepted_reference_window`
- each
  `preserved_replay_input_locator_implementation_output_boundary.bound_replay_input_locator_handle_kind`
- each `replay_input_locator_implementation_disposition`
- `realization_disposition`
- `realization_notes`
- `chosen_replay_input_locator_implementation_contract_shape`

The following are no longer direct input at this boundary:

- replay-input locator implementation contracts below this layer
- replay-input locator implementation reopen decisions below this layer
- replay-input-access/source-binding realizations
- blocked replay-side parse-attempt results
- parse-attempt contracts
- materialization-attempt realizations
- replay-parsing reopen decisions
- replay-parsing-success reopen decisions
- materialization-contract results
- validation results
- lower locator-contract results
- lower locator-realization results
- planning results
- proof results
- emitted-output audit/readback results
- actual emission receipts
- plans
- export-layout results
- specimen batches
- refined specimens
- persisted artifacts
- BC rows
- shells
- lower planning boundaries

Those lower layers remain frozen. This pass starts strictly from the truthful blocked
replay-input locator implementation realization result and the already-preserved audited family root
reference.

## D. DECISION QUESTION

The exact question is:

- should actual replay-input locator implementation work now be deliberately reopened above the
  truthful blocked replay-input locator implementation realization result?

This question exists only because the current repo can now prove it can do no more than a blocked
/ unavailable replay-input locator implementation result.

That means the unresolved gap is no longer whether the implementation-facing replay-input locator
contract exists or whether the truthful blocked implementation result has been made explicit.

The unresolved gap is narrower:

- whether the repo now needs one explicit actual replay-input locator implementation boundary above
  the truthful blocked replay-input locator implementation realization result

This is still not implementation and still not replay parsing.

## E. REOPEN CRITERIA

Reopening is justified only if all of the following hold:

1. the remaining missing piece is truly actual replay-input locator implementation work and not
   another lower-layer contract defect below the truthful blocked replay-input locator
   implementation realization result
2. the next boundary can stay low-boost-recovery-specific
3. the next boundary can stay receipt-bound
4. the next boundary can avoid sidecars, manifests, and generic indexing
5. the next boundary can avoid `mimir_export`
6. the next boundary can stay below actual replay parsing
7. the next boundary can stay below actual raw-state payload materialization, tensor
   materialization, and control/action materialization
8. the next boundary can introduce one actual-implementation-facing handle contract without
   inventing replay-path semantics absent from the admitted realization result
9. the next boundary can preserve the existing truth that:
   - `source_replay` remains opaque lineage only
   - `audited_family_root_directory` remains only a BC specimen-tree anchor
   - `FutureParserConsumableReplayHandleOnly` remains contract-only until a later explicit
     boundary says more
10. the audited crate surfaces do not already supply a richer boundary that would make this reopen
    decision dishonest or redundant:
    - `mimir-skill` already validates the blocked implementation realization boundary
    - `mimir-replay` still exposes only `ReplayInput`, `ReplayHeader`, `ReplayReader`, and
      `UnsupportedReplayReader`
    - `mimir-io` exposes no replay-input locator storage or search framework
    - `mimir-export` remains unrelated and untouched

## F. DECISION

The decision in v1 is:

- reopen justified for one narrow receipt-bound replay-input locator actual-implementation boundary

### Why reopening is justified

Reopening is justified because the truthful blocked replay-input locator implementation result
already isolates the missing piece precisely:

- the repo preserves enough trustworthy receipt-bound identity and lineage to support one next
  narrow actual-implementation-facing boundary
- the repo still does not expose any boundary above the truthful blocked implementation result
  that says how that exact preserved tuple may later enter deliberate replay-input locator
  implementation work

The current repo already preserves enough trustworthy input to define that boundary narrowly:

- `lane_ordinal`
- `specimen_ordinal`
- `artifact_id`
- `anchored_bc_specimen_file_path`
- `source_raw_state_window_ref`
- `source_slice_id`
- `source_replay`
- `source_subject`
- `source_phase_id`
- `preserved_observation_binding_kind`
- `preserved_accepted_reference_window`
- `bound_replay_input_locator_handle_kind`
- the audited family root directory reference

That means the minimum honest reopen is not replay parsing, not raw-state payload
materialization, not tensor/control materialization, and not sidecar/manifest work.

It is one contract-only replay-input locator actual-implementation boundary above the truthful
blocked replay-input locator implementation realization result.

### Narrowest honest next boundary shape

The narrowest honest next boundary shape is:

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorActualImplementationContractV1`

### What it would consume

It would consume only:

- the admitted truthful blocked replay-input locator implementation realization result
- the audited family root directory reference already preserved by that realization result

### What it would expose

It would expose exactly one contract-only replay-input locator actual-implementation-facing
boundary per admitted specimen:

- one actual-implementation-facing handle contract keyed by the exact preserved receipt-bound
  lineage tuple already carried by the truthful blocked implementation realization result

### What remains deferred

This decision still defers:

- actual replay-input locator implementation
- actual replay-input location
- actual `mimir_replay::ReplayInput`
- actual replay parsing
- actual raw-state payload materialization
- tensor materialization
- control/action extraction
- sidecar/manifest realization
- generic indexing
- `mimir_export`

### Why this is the minimum honest reopen

This is the minimum honest reopen because:

- the truthful blocked replay-input locator implementation realization result is already explicit
  and machine-checkable
- the missing problem is now later than blocked implementation realization but earlier than replay
  parsing
- `source_replay` still remains opaque lineage only
- `audited_family_root_directory` still remains only a BC specimen-tree anchor
- no current evidence proves that sidecars, manifests, generic indexing, or `mimir_export`
  belong in the missing defect path

Anything narrower would only restate the already-known blocked truth. Anything broader would
invent replay-storage or replay-path semantics that the admitted realization result still does not
carry.

## G. REPLAY-INPUT LOCATOR ACTUAL-IMPLEMENTATION CONTRACT SHAPE V1

Because reopening is justified, the narrowest contract-only next boundary is fixed here.

### Contract name

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorActualImplementationContractV1`

### Exact inputs it would consume from the blocked replay-input locator implementation realization result

At the top level, the contract would consume exactly:

- `specimen_count`
- `group_count`
- `source_contract_disposition`
- `source_contract_notes`
- `source_chosen_locator_contract_shape`
- `source_chosen_materialization_contract_shape`
- `source_chosen_replay_parsing_contract_shape`
- `source_chosen_replay_input_access_contract_shape`
- `source_chosen_replay_input_locator_contract_shape`
- `audited_family_root_directory`
- `realization_disposition`
- `realization_notes`
- `chosen_replay_input_locator_implementation_contract_shape`

For one admitted specimen, the contract would consume exactly:

- `lane_ordinal`
- `specimen_ordinal`
- `artifact_id`
- `anchored_bc_specimen_file_path`
- `source_raw_state_window_ref`
- `source_slice_id`
- `source_replay`
- `source_subject`
- `source_phase_id`
- `preserved_observation_binding_kind`
- `preserved_accepted_reference_window`
- `bound_replay_input_locator_handle_kind`
- `replay_input_locator_implementation_disposition`

Those values would be consumed only from:

- `consumed_replay_input_locator_implementation_boundary_input`
- `preserved_replay_input_locator_implementation_output_boundary`
- `replay_input_locator_implementation_disposition`

The next contract would not recompute those values from lower layers.

### Exact actual-implementation-facing boundary it would expose

The next contract would expose exactly one per-specimen replay-input locator
actual-implementation-facing boundary that:

- preserves the exact admitted receipt-bound lineage tuple
- preserves the exact deferred observation lineage fields
- preserves that the current handle kind is still
  `FutureParserConsumableReplayHandleOnly`
- adds only one new actual-implementation-facing contract boundary whose admitted meaning is:
  this exact preserved tuple now requires one later explicit replay-input locator implementation
  step before any replay-input location or parsing claim can exist

That next contract may acknowledge that later implementation work might target
`mimir_replay::ReplayInput`, but it must not materialize or promise one in the contract itself.

### Exact invariant

The exact invariant is:

- for one admitted low-boost-recovery truthful blocked replay-input locator implementation
  specimen, the tuple
  (`lane_ordinal`, `specimen_ordinal`, `artifact_id`, `anchored_bc_specimen_file_path`,
  `source_raw_state_window_ref`, `source_slice_id`, `source_replay`, `source_subject`,
  `source_phase_id`, `preserved_observation_binding_kind`,
  `preserved_accepted_reference_window`) plus preserved receipt-bound lineage and the preserved
  `FutureParserConsumableReplayHandleOnly` contract-only handle kind binds to exactly one honest
  replay-input locator actual-implementation-facing handle contract
- that contract is authoritative only for that exact preserved tuple
- that contract must not silently rewrite, drop, pad, or reorder preserved lineage
- that contract must not reinterpret `source_replay.provenance_label` as a path contract
- that contract must not reinterpret `audited_family_root_directory` as replay storage

### Exact relationship to deferred observation access

The relationship to deferred observation access remains narrow:

- `preserved_observation_binding_kind` and `preserved_accepted_reference_window` stay preserved as
  deferred lineage only
- the replay-input locator actual-implementation contract does not reopen observation
  materialization
- the next contract only guarantees that deferred observation lineage stays attached to the same
  specimen while replay-input locator actual-implementation work is being defined

### What it explicitly refuses to promise

The next contract explicitly refuses to promise:

- actual replay-input locator implementation
- actual replay-input location
- actual `mimir_replay::ReplayInput`
- actual replay parsing
- replay frame availability
- replay byte availability
- replay file path derivation from `source_replay`
- replay storage derivation from `audited_family_root_directory`
- actual raw-state payload materialization
- tensor materialization
- control/action extraction
- sidecar/manifest realization
- generic index semantics
- `mimir_export` integration

## H. ADMISSION RULES

A replay-input locator implementation realization result may enter this reopen-decision boundary
only when all of the following hold:

1. the input is exactly
   `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorImplementationRealizationResultV1`
2. `source_contract_disposition ==`
   `ContractDefinedForReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorImplementationOnly`
3. `source_contract_notes` still equal the exact replay-input locator implementation contract note
   set
4. `source_chosen_locator_contract_shape == ReceiptBoundSpecimenFileAnchored`
5. `source_chosen_materialization_contract_shape ==`
   `ReceiptBoundValidatedSpecimenFileSourceRawStateWindowRefMaterializationAttemptOnly`
6. `source_chosen_replay_parsing_contract_shape ==`
   `ReceiptBoundValidatedSpecimenFileSourceRawStateWindowRefReplaySideParseAttemptOnly`
7. `source_chosen_replay_input_access_contract_shape ==`
   `ReceiptBoundReplayInputAccessSourceBindingOnly`
8. `source_chosen_replay_input_locator_contract_shape == ReceiptBoundReplayInputLocatorOnly`
9. `realization_disposition == RealizedForTruthfulBlockedReplayInputLocatorImplementationOnly`
10. `realization_notes` still equal the exact truthful blocked replay-input locator implementation
    realization note set
11. `chosen_replay_input_locator_implementation_contract_shape ==`
    `ReceiptBoundReplayInputLocatorImplementationOnly`
12. `specimen_count > 0`
13. `group_count > 0`
14. `group_count` equals the number of preserved ordered lane results
15. `audited_family_root_directory` still ends in `low_boost_recovery_bc_v1`
16. `audited_family_root_directory` still exists as a directory at decision time
17. lane order and specimen order still match concrete lane/specimen position
18. each
    `consumed_replay_input_locator_implementation_boundary_input.anchored_bc_specimen_file_path`
    still remains receipt-bound below `audited_family_root_directory`
19. each `preserved_replay_input_locator_implementation_output_boundary` still matches the
    corresponding consumed replay-input locator implementation boundary input on:
    - `lane_ordinal`
    - `specimen_ordinal`
    - `artifact_id`
    - `anchored_bc_specimen_file_path`
    - `source_raw_state_window_ref`
    - `source_slice_id`
    - `source_replay`
    - `source_subject`
    - `source_phase_id`
    - `preserved_observation_binding_kind`
    - `preserved_accepted_reference_window`
20. each specimen still preserves
    `replay_input_locator_implementation_disposition ==`
    `RealizedForTruthfulBlockedReplayInputLocatorImplementationOnly`
21. each preserved output boundary still preserves
    `bound_replay_input_locator_handle_kind ==`
    `FutureParserConsumableReplayHandleOnly`
22. no lower boundary is silently reopened to repair or reinterpret the admitted realization input

Admission here means only:

- this reopen decision may rely on the truthful blocked replay-input locator implementation result
  as the last trusted pre-actual-implementation layer

Admission here does not mean:

- replay-input locator implementation is implemented
- replay-input location succeeds
- replay parsing succeeds
- replay files or replay bytes are available
- raw-state payload exists
- tensors or controls are available

## I. FAILURE RULES

This pass must hard-fail for:

- degraded replay-input locator implementation realization input
- count/order/root/path drift
- duplicate or missing artifact ids
- any input/output-boundary identity drift
- any specimen whose replay-input locator implementation disposition no longer remains truthful
  blocked only
- any attempt to reinterpret `source_replay` or `provenance_label` as an implicit replay path
- any attempt to reinterpret the audited family root as replay storage
- any attempt to widen this pass into actual replay-input locator implementation
- any attempt to widen this pass into actual replay parsing
- any attempt to widen this pass into actual raw-state payload materialization
- any attempt to widen this pass into tensor/control materialization
- any attempt to widen this pass into sidecars, manifests, generic indexing, or `mimir_export`

This pass must produce `reopen not justified yet` only if all admission rules still hold but one
or more reopen criteria fail without any admitted-input degradation. In v1 that would mean the
remaining missing piece is still a lower contract defect or the next honest boundary would require
invented replay-path semantics, sidecars, manifests, generic indexing, or `mimir_export`.

This pass must produce `reopen justified` only if all admission rules hold and all reopen criteria
hold. The audited repo state in v1 takes that branch.

## J. NON-GOALS

This pass does not do any of the following:

- no actual replay-input locator implementation
- no actual replay parsing
- no actual raw-state payload materialization
- no tensor materialization
- no control/action extraction
- no `mimir_export` integration
- no sidecar/manifest realization
- no generic manifest/index framework
- no usefulness proof
- no policy-improvement proof
- no replay corpus ingestion
- no rollout or physics work
- no async/background system
- no database work

## K. RELATION TO NEXT STAGES

### What this pass now guarantees

This pass now guarantees:

- the replay-input locator actual-implementation reopen decision is explicit and auditable
- the decision stays low-boost-recovery-specific
- the decision stays receipt-bound
- the narrowest honest next contract shape is fixed:
  `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorActualImplementationContractV1`
- `mimir_export` remains untouched and forbidden

### What remains deferred

This pass still does not guarantee:

- actual replay-input locator implementation
- actual replay-input location
- actual `mimir_replay::ReplayInput`
- actual replay parsing
- actual raw-state-window payload materialization
- tensor/control materialization
- sidecar/manifest necessity

### Immediate next-stage implication

The immediate next pass should be:

- one first narrow contract-definition pass for
  `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorActualImplementationContractV1`
- still above
  `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorImplementationRealizationResultV1`
- still without actual replay-input locator implementation
- still without actual replay parsing
- still without `mimir_export` widening unless that separate boundary is explicitly reopened
