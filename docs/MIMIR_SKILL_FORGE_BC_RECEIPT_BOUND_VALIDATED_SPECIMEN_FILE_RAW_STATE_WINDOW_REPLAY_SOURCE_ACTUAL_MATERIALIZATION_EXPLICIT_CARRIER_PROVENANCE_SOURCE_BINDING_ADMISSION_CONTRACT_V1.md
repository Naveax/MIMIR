# MIMIR Skill Forge BC Receipt-Bound Validated Specimen-File Raw-State Window Replay-Source Actual-Materialization Explicit Carrier Provenance/Source-Binding Admission Contract v1

## A. PURPOSE

### What this pass owns

This pass owns exactly one contract-definition boundary above:

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationCarrierProvenanceBindingRealizationResultV1`

It defines:

- the first broader replay-source-actual-materialization-facing explicit carrier
  provenance/source-binding admission contract surface
- the exact per-specimen admitted input tuple above the truthful blocked
  carrier-provenance/source-binding realization result
- the exact contract-only output boundary that one admitted specimen contributes
- the exact admission and failure rules for defining that contract honestly

### Why it exists

The broader replay-source actual-materialization reopen decision above the truthful blocked
carrier-provenance/source-binding realization already fixed two things:

- reopening is justified
- the narrowest honest next shape is
  `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationExplicitCarrierProvenanceSourceBindingAdmissionContractV1`

What was still missing was the contract surface itself:

- what one admitted truthful blocked carrier-provenance/source-binding realization specimen means
  at the broader replay-source-actual-materialization-facing admission boundary
- what the broader admission boundary consumes and exposes without implementing replay-source
  actual-materialization, carrier discovery, locator logic, parsing, or payload materialization

### How it differs from the broader replay-source actual-materialization reopen-decision boundary below it

- The reopen-decision boundary decided that this contract must exist.
- This pass defines the contract shape itself.
- The reopen-decision boundary did not add a machine-verifiable public contract surface.
- This pass may freeze a machine-verifiable low-boost-recovery-specific contract surface in
  `mimir-skill`.
- Neither boundary implements replay-source actual-materialization, replay-source carrier
  discovery, replay-input locator logic, replay parsing, raw-state payload materialization,
  tensor/control materialization, or `mimir_export` integration.

## B. FAMILY SCOPE

`low_boost_recovery` is the only supported family in this contract version.

This explicit carrier provenance/source-binding admission contract remains family-specific because:

- the admitted input is only
  `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationCarrierProvenanceBindingRealizationResultV1`
- the preserved lineage tuple is only the low-boost-recovery BC specimen tuple already carried by
  that realization result
- the deferred observation lineage is only the low-boost-recovery BC
  observation-binding/accepted-window pair already carried by that realization result
- the preserved handle-kind lineage is only the low-boost-recovery receipt-bound lineage already
  carried by that realization result
- no second family exists that would justify generic replay/raw-state/index/export/materialization
  admission framework work

No generic all-family replay/raw-state/index/export/materialization framework is introduced here.

## C. INPUT BOUNDARY

This pass consumes only:

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationCarrierProvenanceBindingRealizationResultV1`
- the audited family root directory reference already preserved by that realization result
- the broader reopen-decision docs/artifacts as audit guidance only

From
`LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationCarrierProvenanceBindingRealizationResultV1`,
this pass consumes exactly:

- `specimen_count`
- `group_count`
- `source_contract_disposition`
- `source_contract_notes`
- `source_chosen_locator_contract_shape`
- `source_chosen_materialization_contract_shape`
- `source_chosen_replay_parsing_contract_shape`
- `source_chosen_replay_input_access_contract_shape`
- `source_chosen_replay_input_locator_contract_shape`
- `source_chosen_replay_input_locator_implementation_contract_shape`
- `source_chosen_replay_input_locator_actual_implementation_contract_shape`
- `source_chosen_replay_source_materialization_contract_shape`
- `source_chosen_replay_source_actual_materialization_contract_shape`
- `audited_family_root_directory`
- ordered `preserved_ordered_lane_results`
- each `lane_ordinal`
- each ordered `ordered_specimen_results`
- each `specimen_ordinal`
- each
  `consumed_replay_source_actual_materialization_carrier_provenance_binding_boundary_input`
- each
  `preserved_replay_source_actual_materialization_carrier_provenance_binding_output_boundary`
- each `replay_source_actual_materialization_carrier_provenance_binding_disposition`
- `realization_disposition`
- `realization_notes`
- `chosen_replay_source_actual_materialization_carrier_provenance_binding_contract_shape`

Lower contract/reopen/realization layers are no longer direct input here, including:

- broader replay-source actual-materialization reopen-decision layers below the blocked
  carrier-provenance/source-binding realization
- replay-source actual-materialization carrier-provenance/source-binding contracts below this
  realization
- replay-source actual-materialization contracts and realizations below this layer
- replay-source materialization contracts and realizations below this layer
- replay-input locator actual-implementation contracts and realizations below this layer
- replay-input locator implementation contracts and realizations below this layer
- replay-input locator contracts and realizations below this layer
- replay-input-access/source-binding contracts and realizations below this layer
- replay-side parse-attempt contracts and realizations below this layer
- raw-state materialization reopen, contract, and realization layers below this layer
- sidecars, manifests, generic indexes, persisted artifacts, and `mimir_export`

Those lower layers remain frozen. This pass starts strictly from the truthful blocked
carrier-provenance/source-binding realization result and the already-preserved audited family root
reference.

## D. CONTRACT ROLE

This contract owns exactly one thing:

- defining one broader replay-source-actual-materialization-facing admission boundary for an
  externally admitted explicit carrier provenance/source binding keyed by an exact preserved
  low-boost-recovery receipt-bound tuple

This contract is allowed to bind only:

- preserved receipt-bound specimen identity and lineage
- preserved deferred observation lineage
- preserved replay-input-locator handle kind
- preserved replay-source-materialization requirement kind
- preserved replay-source-actual-materialization handle kind
- preserved carrier-provenance/source-binding handle kind
- one new contract-only explicit admission kind that says a later boundary may externally admit an
  explicit carrier provenance/source binding for that exact preserved tuple

This contract is not allowed to implement, discover, locate, parse, or materialize:

- replay-source actual-materialization
- replay-source carrier discovery
- replay-input locator logic
- replay source paths
- replay bytes
- replay frames
- actual `mimir_replay::ReplayInput`
- replay parsing
- raw-state payload materialization
- tensor payloads
- control/action payloads
- sidecars
- manifests
- generic indexes
- `mimir_export`

## E. CONTRACT SHAPE V1

### Contract name

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationExplicitCarrierProvenanceSourceBindingAdmissionContractV1`

### Exact per-specimen admitted inputs

For one admitted specimen, this contract consumes exactly:

- `admitted_replay_source_actual_materialization_carrier_provenance_binding_boundary_input:
  LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationCarrierProvenanceBindingBoundaryInputV1`
- `admitted_replay_source_actual_materialization_carrier_provenance_binding_output_boundary:
  LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationCarrierProvenanceBindingOutputBoundaryV1`
- `admitted_replay_source_actual_materialization_carrier_provenance_binding_disposition:
  LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationCarrierProvenanceBindingRealizationDispositionV1`

Those values are consumed only from the truthful blocked
carrier-provenance/source-binding realization result. They are not recomputed from lower layers.

### Exact broader replay-source-actual-materialization-facing admission boundary input tuple

For one admitted specimen, the broader admission boundary input is exactly:

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationExplicitCarrierProvenanceSourceBindingAdmissionBoundaryInputV1`

It preserves exactly:

- `lane_ordinal`
- `specimen_ordinal`
- `artifact_id`
- `anchored_bc_specimen_file_path`
- `source_raw_state_window_ref`
- `source_slice_id`
- `source_replay`
- `source_subject`
- `source_phase_id`
- `preserved_observation_binding_kind`
- `preserved_accepted_reference_window`
- `preserved_replay_input_locator_handle_kind`
- `preserved_replay_source_materialization_requirement_kind`
- `preserved_replay_source_actual_materialization_handle_kind`
- `preserved_replay_source_actual_materialization_carrier_provenance_binding_handle_kind`

### Exact broader replay-source-actual-materialization-facing admission output-boundary shape

For one admitted specimen, the broader admission boundary output is exactly:

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationExplicitCarrierProvenanceSourceBindingAdmissionOutputBoundaryV1`

It preserves that exact same tuple and adds only:

- `bound_explicit_replay_source_carrier_provenance_source_binding_admission_kind`

The exact new contract-only admission kind is:

- `LowBoostRecoveryBcReceiptBoundReplaySourceActualMaterializationExplicitCarrierProvenanceSourceBindingAdmissionKindV1::FutureAdmittedExplicitReplaySourceCarrierProvenanceSourceBindingOnly`

Its admitted meaning is only:

- this exact preserved tuple may later receive one externally admitted explicit replay-source
  carrier provenance/source binding input at a later realization boundary
- the current lineage tuple is not that admitted input
- the current audited-root presence is not that admitted input
- the contract does not say what the later carrier class is
- the contract does not say where that later carrier lives

### Exact invariant

For one admitted truthful blocked carrier-provenance/source-binding realization specimen, the
tuple

- `lane_ordinal`
- `specimen_ordinal`
- `artifact_id`
- `anchored_bc_specimen_file_path`
- `source_raw_state_window_ref`
- `source_slice_id`
- `source_replay`
- `source_subject`
- `source_phase_id`
- `preserved_observation_binding_kind`
- `preserved_accepted_reference_window`
- `preserved_replay_input_locator_handle_kind`
- `preserved_replay_source_materialization_requirement_kind`
- `preserved_replay_source_actual_materialization_handle_kind`
- `preserved_replay_source_actual_materialization_carrier_provenance_binding_handle_kind`

binds to exactly one honest broader replay-source-actual-materialization explicit carrier
provenance/source-binding admission contract.

That invariant means:

- the contract is authoritative only for that exact preserved tuple
- the contract must not silently rewrite, drop, pad, reorder, or widen preserved lineage
- the contract must not reinterpret `source_replay` as replay source
- the contract must not reinterpret `source_replay.provenance_label` as a replay path contract
- the contract must not reinterpret `audited_family_root_directory` as replay storage
- the contract must not claim an admitted explicit carrier provenance/source binding already
  exists without a later boundary that actually receives one

### Exact relationship to preserved observation lineage

`preserved_observation_binding_kind` remains:

- preserved deferred observation lineage only
- still not observation payload materialization
- still not replay parsing success

`preserved_accepted_reference_window` remains:

- preserved bounded window lineage only
- still not raw-state payload materialization
- still not tensor/control materialization

### Exact relationship to preserved replay/materialization handle kinds

`FutureParserConsumableReplayHandleOnly` remains:

- preserved handle-kind lineage only
- still not replay source
- still not replay path
- still not actual `mimir_replay::ReplayInput`

`ExplicitReplaySourceMaterializationRequiredBeforeReplayInputOrParsing` remains:

- preserved requirement-only lineage only
- still not replay-source actual-materialization success
- still not replay parsing success

`FutureExplicitReplaySourceCarrierOnly` remains:

- preserved handle-kind lineage only
- still not a realized explicit carrier

`FutureExplicitReplaySourceCarrierProvenanceBindingOnly` remains:

- preserved handle-kind lineage only
- still not a realized explicit carrier provenance/source binding

### Exact opacity and anchor rules

`source_replay` remains opaque lineage unless and until a later explicit boundary says otherwise.

`source_replay.provenance_label` remains opaque lineage and is not a replay path contract.

`audited_family_root_directory` remains only a BC specimen-tree anchor.

This contract may acknowledge future externally admitted explicit carrier provenance/source
binding without materializing or promising:

- replay path
- replay bytes
- replay frames
- actual `mimir_replay::ReplayInput`

## F. CONTRACT OUTPUT V1

The minimum family-specific contract-definition result is:

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationExplicitCarrierProvenanceSourceBindingAdmissionContractV1`

It contains exactly:

- preserved `specimen_count`
- preserved `group_count`
- preserved `source_contract_disposition`
- preserved `source_contract_notes`
- preserved `source_chosen_locator_contract_shape`
- preserved `source_chosen_materialization_contract_shape`
- preserved `source_chosen_replay_parsing_contract_shape`
- preserved `source_chosen_replay_input_access_contract_shape`
- preserved `source_chosen_replay_input_locator_contract_shape`
- preserved `source_chosen_replay_input_locator_implementation_contract_shape`
- preserved `source_chosen_replay_input_locator_actual_implementation_contract_shape`
- preserved `source_chosen_replay_source_materialization_contract_shape`
- preserved `source_chosen_replay_source_actual_materialization_contract_shape`
- preserved `source_chosen_replay_source_actual_materialization_carrier_provenance_binding_contract_shape`
- preserved `audited_family_root_directory`
- preserved ordered lane results
- preserved lane order
- preserved specimen order
- preserved artifact ids
- one per-specimen broader explicit admission boundary result
- bounded `contract_disposition`
- bounded `contract_notes`
- chosen explicit carrier provenance/source-binding admission contract shape

Each per-specimen result contains exactly:

- `specimen_ordinal`
- `admitted_replay_source_actual_materialization_carrier_provenance_binding_boundary_input`
- `admitted_replay_source_actual_materialization_carrier_provenance_binding_output_boundary`
- `admitted_replay_source_actual_materialization_carrier_provenance_binding_disposition`
- `replay_source_actual_materialization_explicit_carrier_provenance_source_binding_admission_boundary_input`
- `replay_source_actual_materialization_explicit_carrier_provenance_source_binding_admission_output_boundary`

The exact top-level disposition is:

- `ContractDefinedForReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationExplicitCarrierProvenanceSourceBindingAdmissionOnly`

The exact chosen shape is:

- `ReceiptBoundReplaySourceActualMaterializationExplicitCarrierProvenanceSourceBindingAdmissionOnly`

## G. ADMISSION RULES

The truthful blocked carrier-provenance/source-binding realization result may enter this contract
boundary only when all of the following hold:

1. the input remains an exact
   `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationCarrierProvenanceBindingRealizationResultV1`
2. `source_contract_disposition ==`
   `ContractDefinedForReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationCarrierProvenanceBindingOnly`
3. `source_contract_notes` remain the exact frozen carrier-provenance/source-binding contract
   note set
4. `source_chosen_locator_contract_shape == ReceiptBoundSpecimenFileAnchored`
5. `source_chosen_materialization_contract_shape ==`
   `ReceiptBoundValidatedSpecimenFileSourceRawStateWindowRefMaterializationAttemptOnly`
6. `source_chosen_replay_parsing_contract_shape ==`
   `ReceiptBoundValidatedSpecimenFileSourceRawStateWindowRefReplaySideParseAttemptOnly`
7. `source_chosen_replay_input_access_contract_shape ==`
   `ReceiptBoundReplayInputAccessSourceBindingOnly`
8. `source_chosen_replay_input_locator_contract_shape == ReceiptBoundReplayInputLocatorOnly`
9. `source_chosen_replay_input_locator_implementation_contract_shape ==`
   `ReceiptBoundReplayInputLocatorImplementationOnly`
10. `source_chosen_replay_input_locator_actual_implementation_contract_shape ==`
    `ReceiptBoundReplayInputLocatorActualImplementationOnly`
11. `source_chosen_replay_source_materialization_contract_shape ==`
    `ReceiptBoundReplaySourceMaterializationOnly`
12. `source_chosen_replay_source_actual_materialization_contract_shape ==`
    `ReceiptBoundReplaySourceActualMaterializationOnly`
13. `chosen_replay_source_actual_materialization_carrier_provenance_binding_contract_shape ==`
    `ReceiptBoundReplaySourceActualMaterializationCarrierProvenanceBindingOnly`
14. `realization_disposition ==`
    `RealizedForTruthfulBlockedReplaySourceActualMaterializationCarrierProvenanceBindingOnly`
15. `realization_notes` remain the exact frozen truthful blocked carrier-provenance/source-binding
    realization note set
16. `specimen_count > 0`
17. `group_count > 0`
18. `group_count` equals the number of preserved ordered lane results
19. `audited_family_root_directory` still exists as a directory and still ends in
    `low_boost_recovery_bc_v1`
20. lane order and specimen order still match concrete lane/specimen position
21. each consumed carrier-provenance/source-binding boundary input and each preserved
    carrier-provenance/source-binding output boundary still remain receipt-bound below
    `audited_family_root_directory`
22. each preserved carrier-provenance/source-binding output boundary still matches the consumed
    carrier-provenance/source-binding boundary input on:
    - `lane_ordinal`
    - `specimen_ordinal`
    - `artifact_id`
    - `anchored_bc_specimen_file_path`
    - `source_raw_state_window_ref`
    - `source_slice_id`
    - `source_replay`
    - `source_subject`
    - `source_phase_id`
    - `preserved_observation_binding_kind`
    - `preserved_accepted_reference_window`
    - `preserved_replay_input_locator_handle_kind`
    - `preserved_replay_source_materialization_requirement_kind`
    - `preserved_replay_source_actual_materialization_handle_kind`
23. every preserved replay-input-locator handle kind still remains
    `FutureParserConsumableReplayHandleOnly`
24. every preserved replay-source-materialization requirement kind still remains
    `ExplicitReplaySourceMaterializationRequiredBeforeReplayInputOrParsing`
25. every preserved replay-source-actual-materialization handle kind still remains
    `FutureExplicitReplaySourceCarrierOnly`
26. every preserved carrier-provenance/source-binding handle kind still remains
    `FutureExplicitReplaySourceCarrierProvenanceBindingOnly`
27. every specimen still preserves
    `replay_source_actual_materialization_carrier_provenance_binding_disposition ==`
    `RealizedForTruthfulBlockedReplaySourceActualMaterializationCarrierProvenanceBindingOnly`

Admission here means only:

- this boundary may define one contract-only future explicit carrier provenance/source-binding
  admission surface for the exact preserved tuple

Admission here does not mean:

- explicit replay-source carrier provenance/source binding already exists
- replay-source actual-materialization succeeds
- replay-source carrier discovery succeeds
- replay-input locator logic succeeds
- replay parsing succeeds
- replay files, replay bytes, or replay frames are available
- raw-state payload exists
- tensors or controls exist

## H. FAILURE RULES

This boundary must hard-fail for:

- degraded realization input
- degraded source-contract-note or realization-note sets
- mismatched counts
- mismatched lane order
- mismatched specimen order
- mismatched artifact ids
- duplicate artifact ids
- audited family root drift
- audited family root missing or not a directory
- anchored specimen-file path drift outside the audited family root
- any drift in:
  - `lane_ordinal`
  - `specimen_ordinal`
  - `artifact_id`
  - `anchored_bc_specimen_file_path`
  - `source_raw_state_window_ref`
  - `source_slice_id`
  - `source_replay`
  - `source_subject`
  - `source_phase_id`
  - `preserved_observation_binding_kind`
  - `preserved_accepted_reference_window`
  - `preserved_replay_input_locator_handle_kind`
  - `preserved_replay_source_materialization_requirement_kind`
  - `preserved_replay_source_actual_materialization_handle_kind`
  - `preserved_replay_source_actual_materialization_carrier_provenance_binding_handle_kind`
- any attempt to reinterpret `source_replay` or `source_replay.provenance_label` as an implicit
  replay path or replay source
- any attempt to reinterpret the audited family root as replay storage
- any attempt to widen this pass into replay-source actual-materialization implementation
- any attempt to widen this pass into replay-source carrier discovery
- any attempt to widen this pass into replay-input locator logic
- any attempt to widen this pass into replay parsing
- any attempt to widen this pass into actual raw-state payload materialization
- any attempt to widen this pass into tensor/control materialization

This v1 boundary is intentionally strict:

- no repair
- no specimen skipping
- no resorting
- no replay-path guessing
- no replay-byte guessing
- no replay-carrier synthesis
- no sidecar/manifest/index fallback

## I. NON-GOALS

This pass does not do any of the following:

- no replay-source actual-materialization implementation
- no replay-source carrier discovery
- no replay-input locator logic
- no replay parsing
- no actual raw-state payload materialization
- no tensor materialization
- no control/action extraction
- no `mimir_export` integration
- no sidecar/manifest realization unless separately justified
- no generic manifest/index framework
- no usefulness proof
- no policy-improvement proof

## J. RELATION TO NEXT STAGES

This pass now guarantees:

- one explicit replay-source-actual-materialization-facing admission contract exists above the
  truthful blocked carrier-provenance/source-binding realization result
- that contract stays low-boost-recovery-specific
- that contract stays receipt-bound
- the preserved receipt-bound identity tuple, deferred observation lineage, and preserved
  handle-kind lineage remain visible without inventing replay storage, replay paths, replay bytes,
  replay frames, or replay payload semantics
- one contract-only future admission kind now makes the next missing realization problem explicit:
  a later boundary may externally admit an explicit carrier provenance/source binding for the
  exact preserved tuple
- `mimir_export` remains untouched and forbidden

This pass still does not guarantee:

- admitted explicit replay-source carrier provenance/source binding exists
- replay-source actual-materialization succeeds
- replay-source carrier discovery succeeds
- replay-input locator logic succeeds
- replay parsing succeeds
- replay bytes or replay frames exist
- actual raw-state-window payload materialization
- tensor/control materialization

The immediate next pass should be:

- one first realization pass above
  `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationExplicitCarrierProvenanceSourceBindingAdmissionContractV1`

That next pass is now obvious because this pass freezes the exact contract surface and leaves only
one truthful question above it:

- what is the first honest realization result above this contract when no explicit externally
  admitted carrier provenance/source binding input has yet been provided?

`mimir_export` widening remains forbidden unless a later explicit reopen says otherwise.
