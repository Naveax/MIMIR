# MIMIR Skill Forge BC Byte-Backed Caller-Admitted Source-Form Contract v1

## A. PURPOSE

### What this pass owns

This pass owns exactly one contract-definition boundary above:

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationExplicitCarrierProvenanceSourceBindingAdmissionRealizationResultV1`

It defines exactly one new family-specific caller seam in `mimir-skill`:

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationByteBackedCallerAdmittedSourceFormContractV1`

It freezes only:

- one exact byte-backed caller-admitted source-form contract
- one exact caller-admitted object shape
- one exact receipt-bound binding from the preserved tuple to that object shape
- one exact bounded contract result above the truthful blocked explicit-admission realization

### Why it exists

This pass exists because a new explicit external requirement now reopens the chain honestly.

That requirement is:

- one exact caller-admitted source form must now be supported
- the exact chosen source form is byte-backed caller admission
- the exact ownership seam is `mimir-skill`

This pass is therefore justified by explicit external requirement, not by repo-only evidence.

### What this pass does not claim

This pass does not claim that repo-only evidence proved byte-backed admission first.

It claims only:

- the earlier repo-only stop was honest
- the chain is now reopened by an external requirement
- the narrowest honest response is to freeze one exact byte-backed caller contract above the blocked explicit-admission realization

## B. FAMILY SCOPE

`low_boost_recovery` is the only supported family in this contract version.

This remains family-specific because:

- the only lower input is the low-boost-recovery explicit-admission realization result
- the preserved receipt-bound tuple is the low-boost-recovery BC specimen tuple already frozen below
- the preserved observation lineage is the low-boost-recovery BC observation binding and accepted reference window
- the preserved replay/materialization handle lineage is the low-boost-recovery receipt-bound lineage already carried below
- no second family exists that justifies a generic replay/raw-state/index/export/materialization caller-admission framework

No generic all-family replay/raw-state/index/export/materialization framework is introduced here.

## C. INPUT BOUNDARY

This pass consumes only:

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationExplicitCarrierProvenanceSourceBindingAdmissionRealizationResultV1`
- the audited family root directory reference already preserved by that realization result
- the explicit external requirement selecting byte-backed caller admission in `mimir-skill`

From `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationExplicitCarrierProvenanceSourceBindingAdmissionRealizationResultV1`, this pass consumes exactly:

- `specimen_count`
- `group_count`
- `source_contract_disposition`
- `source_contract_notes`
- `source_chosen_locator_contract_shape`
- `source_chosen_materialization_contract_shape`
- `source_chosen_replay_parsing_contract_shape`
- `source_chosen_replay_input_access_contract_shape`
- `source_chosen_replay_input_locator_contract_shape`
- `source_chosen_replay_input_locator_implementation_contract_shape`
- `source_chosen_replay_input_locator_actual_implementation_contract_shape`
- `source_chosen_replay_source_materialization_contract_shape`
- `source_chosen_replay_source_actual_materialization_contract_shape`
- `source_chosen_replay_source_actual_materialization_carrier_provenance_binding_contract_shape`
- `audited_family_root_directory`
- ordered `preserved_ordered_lane_results`
- each `lane_ordinal`
- each ordered `ordered_specimen_results`
- each `specimen_ordinal`
- each admitted explicit-admission boundary input/output pair
- each explicit-admission realization disposition
- `realization_disposition`
- `realization_notes`
- `chosen_replay_source_actual_materialization_explicit_carrier_provenance_source_binding_admission_contract_shape`

Lower contract, reopen, and realization layers are no longer direct input here.

This pass starts strictly from:

- the truthful blocked explicit-admission realization result
- the already-preserved audited family root reference
- the externally imposed byte-backed caller-form requirement

## D. CONTRACT ROLE

This contract owns exactly one thing:

- freezing one exact byte-backed caller-admitted source-form contract for the preserved receipt-bound tuple

This contract is allowed to bind only:

- preserved receipt-bound specimen identity
- preserved source lineage
- preserved observation lineage
- preserved replay/materialization handle lineage
- preserved explicit-admission kind lineage
- one exact future caller-admitted byte-backed object shape

This contract is not allowed to implement, discover, locate, parse, or materialize:

- replay-source actual-materialization
- replay-source carrier discovery
- replay-input locator logic
- replay path derivation
- replay parsing
- raw-state payload parsing
- tensor/control materialization
- corpus ingestion
- `mimir_export` integration
- `mimir_replay::ReplayInput`

This contract stays below `mimir_replay::ReplayInput` and below `mimir_export`.

## E. EXACT CONTRACT SHAPE V1

### Contract name

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationByteBackedCallerAdmittedSourceFormContractV1`

### Exact caller-admitted boundary input tuple

For one specimen, the contract-only boundary input is:

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationByteBackedCallerAdmittedSourceFormBoundaryInputV1`

It preserves exactly:

- `lane_ordinal`
- `specimen_ordinal`
- `artifact_id`
- `anchored_bc_specimen_file_path`
- `source_raw_state_window_ref`
- `source_slice_id`
- `source_replay`
- `source_subject`
- `source_phase_id`
- `preserved_observation_binding_kind`
- `preserved_accepted_reference_window`
- `preserved_replay_input_locator_handle_kind`
- `preserved_replay_source_materialization_requirement_kind`
- `preserved_replay_source_actual_materialization_handle_kind`
- `preserved_replay_source_actual_materialization_carrier_provenance_binding_handle_kind`
- `bound_explicit_replay_source_carrier_provenance_source_binding_admission_kind`

That tuple is derived only from the admitted explicit-admission output boundary already preserved by the truthful blocked realization result.

### Exact caller-admitted boundary output tuple

For one specimen, the contract-only boundary output is:

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationByteBackedCallerAdmittedSourceFormOutputBoundaryV1`

It preserves the exact same tuple and adds only:

- `bound_byte_backed_caller_admitted_source_form_kind`

The exact bound kind is:

- `LowBoostRecoveryBcReceiptBoundReplaySourceActualMaterializationByteBackedCallerAdmittedSourceFormKindV1::CallerAdmittedReplayBytesOnly`

Its exact meaning is only:

- a later boundary may admit replay bytes directly from the caller for this exact preserved tuple
- the later admitted bytes must bind to this exact receipt-bound tuple
- the current contract does not claim those bytes are already present

### Exact caller-admitted object shape

The exact future caller-admitted object is:

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationByteBackedCallerAdmittedSourceFormV1`

It carries exactly:

- `lane_ordinal`
- `specimen_ordinal`
- `artifact_id`
- `anchored_bc_specimen_file_path`
- `source_raw_state_window_ref`
- `source_slice_id`
- `source_replay`
- `source_subject`
- `source_phase_id`
- `preserved_observation_binding_kind`
- `preserved_accepted_reference_window`
- `preserved_replay_input_locator_handle_kind`
- `preserved_replay_source_materialization_requirement_kind`
- `preserved_replay_source_actual_materialization_handle_kind`
- `preserved_replay_source_actual_materialization_carrier_provenance_binding_handle_kind`
- `bound_explicit_replay_source_carrier_provenance_source_binding_admission_kind`
- `caller_admitted_replay_bytes: Vec<u8>`

### How replay bytes are carried

Replay bytes are carried only as:

- `caller_admitted_replay_bytes: Vec<u8>`

Those bytes are:

- caller-admitted only
- opaque byte payload only
- not replay parsing success
- not raw-state payload materialization
- not `mimir_replay::ReplayInput`

### How receipt-bound identity is carried

Receipt-bound identity is carried directly inside the exact caller-admitted object through:

- `lane_ordinal`
- `specimen_ordinal`
- `artifact_id`
- `anchored_bc_specimen_file_path`

### How preserved lineage is carried

Preserved lineage is carried directly inside the exact caller-admitted object through:

- `source_raw_state_window_ref`
- `source_slice_id`
- `source_replay`
- `source_subject`
- `source_phase_id`
- `preserved_observation_binding_kind`
- `preserved_accepted_reference_window`
- `preserved_replay_input_locator_handle_kind`
- `preserved_replay_source_materialization_requirement_kind`
- `preserved_replay_source_actual_materialization_handle_kind`
- `preserved_replay_source_actual_materialization_carrier_provenance_binding_handle_kind`
- `bound_explicit_replay_source_carrier_provenance_source_binding_admission_kind`

### Exact binding statement

For one specimen, the exact preserved tuple:

- `artifact_id`
- `lane_ordinal`
- `specimen_ordinal`
- `anchored_bc_specimen_file_path`
- `source_raw_state_window_ref`
- `source_slice_id`
- `source_replay`
- `source_subject`
- `source_phase_id`
- preserved observation binding
- preserved accepted reference window
- preserved replay-input-locator handle kind
- preserved replay-source-materialization requirement kind
- preserved replay-source-actual-materialization handle kind
- preserved carrier-provenance/source-binding handle kind
- preserved explicit-admission kind

binds to exactly one caller-admitted byte-backed source form:

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationByteBackedCallerAdmittedSourceFormV1`

That binding does not mean:

- replay parsing
- replay-source carrier discovery
- replay path
- replay file location
- replay-input locator success
- replay-source actual-materialization success

### Exact opacity rules

`source_replay` remains opaque lineage only.

`source_replay.provenance_label` remains opaque lineage only and is not a replay path contract.

`audited_family_root_directory` remains only a BC specimen-tree anchor and is not replay storage.

## F. CONTRACT OUTPUT V1

The minimum contract-definition result is:

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationByteBackedCallerAdmittedSourceFormContractV1`

It preserves exactly:

- `specimen_count`
- `group_count`
- all source shape selections already frozen below
- `audited_family_root_directory`
- `source_realization_disposition`
- `source_realization_notes`
- ordered lane/specimen results
- artifact ids

It adds only:

- one per-specimen byte-backed caller-admitted source-form boundary input/output pair
- bounded `contract_disposition`
- bounded `contract_notes`
- `chosen_replay_source_actual_materialization_byte_backed_caller_admitted_source_form_contract_shape`

The exact top-level disposition is:

- `ContractDefinedForReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationByteBackedCallerAdmittedSourceFormOnly`

The exact chosen shape is:

- `ReceiptBoundReplaySourceActualMaterializationByteBackedCallerAdmittedSourceFormOnly`

## G. ADMISSION RULES

The blocked explicit-admission realization result may enter this contract-definition boundary only when all of the following hold:

1. the input remains exactly `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationExplicitCarrierProvenanceSourceBindingAdmissionRealizationResultV1`
2. the lower explicit-admission contract disposition remains the frozen explicit-admission-only disposition
3. the lower explicit-admission contract notes remain the exact frozen explicit-admission contract note set
4. every lower chosen shape remains the exact frozen receipt-bound shape already required below
5. the lower realization disposition remains the truthful blocked explicit-admission-only disposition
6. the lower realization notes remain the exact frozen explicit-admission realization note set
7. the lower explicit-admission chosen shape remains the exact explicit-admission-only shape
8. `specimen_count > 0`
9. `group_count > 0`
10. `group_count` equals the number of preserved ordered lane results
11. `audited_family_root_directory` still exists, is still a directory, and still ends in `low_boost_recovery_bc_v1`
12. lane order and specimen order still match concrete position
13. each anchored specimen path still remains receipt-bound below the audited family root
14. each accepted reference window still remains valid and bounded
15. each preserved replay-input-locator handle kind still remains `FutureParserConsumableReplayHandleOnly`
16. each preserved replay-source-materialization requirement kind still remains `ExplicitReplaySourceMaterializationRequiredBeforeReplayInputOrParsing`
17. each preserved replay-source-actual-materialization handle kind still remains `FutureExplicitReplaySourceCarrierOnly`
18. each preserved carrier-provenance/source-binding handle kind still remains `FutureExplicitReplaySourceCarrierProvenanceBindingOnly`
19. each preserved explicit-admission kind still remains `FutureAdmittedExplicitReplaySourceCarrierProvenanceSourceBindingOnly`
20. every artifact id remains unique

Admission here means only:

- the lower blocked realization may be revalidated
- the exact byte-backed caller object shape may be frozen
- a later realization pass may receive caller bytes for this exact tuple

## H. FAILURE RULES

This boundary must hard-fail for:

- degraded source contract notes
- degraded source realization notes
- chosen-shape drift
- disposition drift
- mismatched counts
- mismatched lane order
- mismatched specimen order
- duplicate artifact ids
- audited root drift
- specimen path drift outside the audited root
- accepted-window invalidity
- any drift in the preserved tuple fields
- any drift in the preserved explicit-admission kind
- any attempt to reinterpret `source_replay` as replay path or replay storage
- any attempt to reinterpret `source_replay.provenance_label` as replay path
- any attempt to reinterpret `audited_family_root_directory` as replay storage
- any attempt to widen this pass into replay-source actual-materialization implementation
- any attempt to widen this pass into replay-source carrier discovery
- any attempt to widen this pass into replay-input locator logic
- any attempt to widen this pass into replay parsing
- any attempt to widen this pass into raw-state payload parsing or tensor/control materialization

This v1 boundary is intentionally strict:

- no repair
- no specimen skipping
- no resorting
- no replay-path guessing
- no replay-byte synthesis
- no sidecar or manifest fallback

## I. NON-GOALS

This pass does not do any of the following:

- no replay-source actual-materialization implementation
- no replay-source carrier discovery
- no replay-input locator logic
- no replay parsing
- no raw-state payload parsing
- no tensor/control materialization
- no `mimir_export` integration
- no replay-source carrier discovery framework
- no replay-input locator framework
- no generic caller-admission framework

It also does not add:

- replay-source actual bytes at runtime
- parser-success logic
- corpus ingestion
- database code
- async/background systems

## J. RELATION TO NEXT STAGES

This pass now guarantees:

- one exact byte-backed caller-admitted source-form contract exists in `mimir-skill`
- the exact future caller-admitted object shape is frozen
- the exact preserved tuple that future bytes must bind to is frozen
- the contract remains low-boost-recovery-specific
- the contract remains receipt-bound
- the contract remains below `mimir_replay::ReplayInput`
- `mimir_export` remains untouched and forbidden

This pass still does not guarantee:

- caller bytes are already present
- replay-source actual-materialization succeeds
- replay-source carrier discovery succeeds
- replay-input locator logic succeeds
- replay parsing succeeds
- raw-state payload materialization
- tensor/control materialization

The immediate next pass is now:

- the first realization pass for this exact byte-backed caller-admitted source-form contract

That next pass may admit concrete byte-backed caller objects matching:

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationByteBackedCallerAdmittedSourceFormV1`

and must still keep:

- `mimir_export` widening forbidden unless explicitly reopened
- replay parsing deferred unless separately reopened
