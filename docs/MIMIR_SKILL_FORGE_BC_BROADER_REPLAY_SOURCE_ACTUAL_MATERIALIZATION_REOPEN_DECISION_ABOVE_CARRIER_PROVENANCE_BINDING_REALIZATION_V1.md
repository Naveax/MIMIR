# MIMIR Skill Forge BC Broader Replay-Source Actual-Materialization Reopen Decision Above Carrier-Provenance Binding Realization v1

## A. PURPOSE

### What this pass owns

This pass owns exactly one reopen-decision question above:

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationCarrierProvenanceBindingRealizationResultV1`

It defines:

- whether one broader replay-source actual-materialization-related boundary must now be
  deliberately reopened at all above the truthful blocked carrier-provenance/source-binding
  realization result
- whether the missing boundary is now specifically explicit carrier provenance/source binding
  admission rather than another lower-chain defect
- the narrowest honest next contract shape only if reopening is justified

### Why it exists

The realization boundary below this pass already fixed the current truthful limit:

- `RealizedForTruthfulBlockedReplaySourceActualMaterializationCarrierProvenanceBindingOnly`

That matters because the repo can now prove all of the following from one admitted
machine-verifiable result:

- the admitted low-boost-recovery receipt-bound specimen identity survives into the truthful
  blocked carrier-provenance/source-binding result
- deferred observation lineage survives into that same result
- the preserved replay-input-locator handle kind survives into that same result
- the preserved replay-source-materialization requirement kind survives into that same result
- the preserved replay-source-actual-materialization handle kind survives into that same result
- the preserved carrier-provenance/source-binding handle kind survives into that same result
- the admitted result still exposes no explicit replay-source carrier provenance/source binding
- the admitted result still exposes no replay path
- the admitted result still exposes no replay bytes
- the admitted result still exposes no replay frames
- the admitted result still exposes no actual `mimir_replay::ReplayInput`
- `source_replay` still remains opaque lineage only
- `source_replay.provenance_label` still remains opaque lineage only
- `audited_family_root_directory` still remains only a BC specimen-tree anchor
- the truthful blocked realization already says the remaining gap is
  `ExplicitReplaySourceCarrierProvenanceSourceBindingUnavailableWithoutAdmittedBindingSource`

This pass exists because the repo can now prove something narrower than before:

- the current admitted contract/result chain is no longer blocked on missing
  carrier-provenance/source-binding contract shape
- the current admitted contract/result chain is no longer blocked on missing truthful blocked
  realization shape above that contract
- the remaining gap is now whether one broader replay-source actual-materialization-related
  boundary must be reopened so one explicit carrier provenance/source binding input can be
  admitted honestly

### How it differs from the carrier-provenance/source-binding realization boundary below it

- The lower boundary realizes the current truth:
  `RealizedForTruthfulBlockedReplaySourceActualMaterializationCarrierProvenanceBindingOnly`.
- This pass does not realize more replay-source semantics.
- This pass decides whether one broader replay-source actual-materialization-related boundary
  must now be deliberately reopened above that truthful blocked result.
- This pass is still not replay-source actual-materialization implementation.
- This pass is still not replay-source carrier discovery.
- This pass is still not replay-input locator logic.
- This pass is still not replay parsing.
- This pass is still not raw-state payload materialization.

## B. FAMILY SCOPE

`low_boost_recovery` is the only supported family in this reopen-decision version.

This broader replay-source actual-materialization reopen decision remains family-specific because:

- the admitted input is only
  `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationCarrierProvenanceBindingRealizationResultV1`
- the preserved lineage tuple is only the low-boost-recovery BC specimen tuple already carried by
  that result
- the preserved deferred observation lineage is only the low-boost-recovery BC
  observation-binding pair already carried by that result
- the preserved handle-kind lineage is only the low-boost-recovery receipt-bound lineage already
  carried by that result
- no second family exists that would justify a shared replay/raw-state/index/export/materialization
  framework

No generic all-family replay/raw-state/index/export/materialization framework is introduced here.

## C. INPUT BOUNDARY

This pass consumes only:

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationCarrierProvenanceBindingRealizationResultV1`
- the audited family root directory reference already preserved by that result

From
`LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationCarrierProvenanceBindingRealizationResultV1`,
this pass consumes exactly:

- `specimen_count`
- `group_count`
- `source_contract_disposition`
- `source_contract_notes`
- `source_chosen_locator_contract_shape`
- `source_chosen_materialization_contract_shape`
- `source_chosen_replay_parsing_contract_shape`
- `source_chosen_replay_input_access_contract_shape`
- `source_chosen_replay_input_locator_contract_shape`
- `source_chosen_replay_input_locator_implementation_contract_shape`
- `source_chosen_replay_input_locator_actual_implementation_contract_shape`
- `source_chosen_replay_source_materialization_contract_shape`
- `source_chosen_replay_source_actual_materialization_contract_shape`
- `audited_family_root_directory`
- ordered `preserved_ordered_lane_results`
- each `lane_ordinal`
- each ordered `ordered_specimen_results`
- each `specimen_ordinal`
- each
  `consumed_replay_source_actual_materialization_carrier_provenance_binding_boundary_input`
- each
  `preserved_replay_source_actual_materialization_carrier_provenance_binding_output_boundary`
- each `replay_source_actual_materialization_carrier_provenance_binding_disposition`
- `realization_disposition`
- `realization_notes`
- `chosen_replay_source_actual_materialization_carrier_provenance_binding_contract_shape`

The following are no longer direct input at this boundary:

- the broader replay-source actual-materialization reopen-decision layer below this realization
- the replay-source actual-materialization carrier-provenance/source-binding contract-definition
  boundary below this realization
- replay-source actual-materialization contracts and realizations below this layer
- replay-source materialization contracts and realizations below this layer
- replay-input locator actual-implementation contracts and realizations below this layer
- replay-input locator implementation contracts and realizations below this layer
- replay-input locator contracts and realizations below this layer
- replay-input-access/source-binding contracts and realizations below this layer
- replay-side parse-attempt contracts and realizations below this layer
- raw-state materialization reopen, contract, and realization layers below this layer
- replay parsing reopen and success-reopen layers below this layer
- sidecars, manifests, generic indexes, persisted artifacts, and `mimir_export`

Those lower layers remain frozen. This pass starts strictly from the truthful blocked
carrier-provenance/source-binding realization result and the already-preserved audited family root
reference.

## D. DECISION QUESTION

The exact question is:

- should one broader replay-source actual-materialization-related boundary now be deliberately
  reopened above the truthful blocked carrier-provenance/source-binding realization result?

This question exists only because the current repo can now prove something stricter than before:

- the current admitted contract/result chain preserves receipt-bound identity, deferred
  observation lineage, the future parser-consumable replay-handle contract fact, the explicit
  replay-source-materialization requirement kind, the contract-only future explicit replay-source
  carrier handle kind, and the contract-only future explicit replay-source carrier
  provenance/source-binding handle kind
- the same chain still cannot honestly yield any explicit replay-source carrier provenance/source
  binding, replay path, replay bytes, replay frames, or actual `mimir_replay::ReplayInput`
- the truthful blocked result now says the gap is lack of an admitted explicit carrier
  provenance/source binding input, not lack of a blocked-result shape

That means the unresolved gap is no longer:

- whether carrier-provenance/source-binding contract shape exists below this layer
- whether truthful blocked carrier-provenance/source-binding realization shape exists below this
  layer
- whether the repo can truthfully say the current admitted result is blocked

The unresolved gap is now later than truthful blocked carrier-provenance/source-binding
realization but earlier than replay parsing:

- whether the repo must now reopen one explicit broader boundary that can admit explicit carrier
  provenance/source binding input without inventing it from opaque lineage or audited-root
  presence alone

## E. REOPEN CRITERIA

Reopening is justified only if all of the following hold:

1. the remaining missing piece is truly a broader replay-source actual-materialization-related
   admission boundary for explicit carrier provenance/source binding and not another defect in the
   lower replay-source chain
2. the next boundary can stay low-boost-recovery-specific
3. the next boundary can stay receipt-bound
4. the next boundary can avoid `mimir_export`
5. the next boundary can stay below replay parsing
6. the next boundary can stay below raw-state payload materialization, tensor materialization, and
   control/action materialization
7. the next boundary can introduce one explicit admission boundary for carrier provenance/source
   binding without cheating through `source_replay`, `source_replay.provenance_label`, or
   `audited_family_root_directory`
8. the audited crate surfaces do not already supply a richer honest carrier provenance/source
   binding admission boundary that would make this reopen dishonest or redundant:
   - `mimir-skill` already makes the blocked carrier-provenance/source-binding result
     machine-checkable
   - `mimir-replay` still exposes only `ReplayInput`, `ReplayHeader`, `ReplayReader`, and
     `UnsupportedReplayReader`
   - `mimir-io` still exposes raw artifact/specimen read/write helpers and no replay-source
     locator or carrier-provenance admission framework
   - `mimir-export` still remains unrelated and untouched
   - `mimir-types::ReplaySourceRef` still contains only `replay_id` and `provenance_label`
9. sidecars, manifests, and generic indexing remain unjustified unless the audited repo state now
   proves one of them is the minimum honest way to admit explicit carrier provenance/source
   binding input
10. the next boundary can remain contract-only in the immediate next pass
11. the broader reopen still remains narrower than replay-source actual-materialization
    implementation, replay-source carrier discovery, replay-input locator logic, replay parsing,
    raw-state payload materialization, tensor/control materialization, corpus-wide replay
    ingestion, sidecar realization, and generic framework work

The audited repo state in v1 satisfies those criteria.

## F. DECISION

The decision in v1 is:

- reopen justified for one narrow receipt-bound broader replay-source actual-materialization-
  related boundary

### Why reopening is justified

Reopening is justified because the truthful blocked carrier-provenance/source-binding result
already isolates the missing piece precisely:

- the repo preserves enough trustworthy receipt-bound identity, deferred observation lineage, and
  preserved handle-kind lineage to bind one later explicit carrier provenance/source-binding
  admission boundary
- the repo still has no honest way to move from that preserved tuple to an admitted explicit
  carrier provenance/source binding input without violating the opacity/root rules
- replay parsing still sits downstream of that missing broader admission boundary

The audited repo state also shows that the missing boundary is not already implemented elsewhere:

- `mimir-replay` exposes a replay-input type, but the current admitted result cannot truthfully
  produce one
- `mimir-io` does not provide replay-source carrier lookup or explicit provenance/source-binding
  admission
- `mimir-export` is unrelated to the missing defect path and remains forbidden

That means the minimum honest reopen is not:

- another lower replay-source-chain repair
- replay-source actual-materialization implementation
- replay-source carrier discovery
- replay-input locator logic
- replay parsing
- raw-state payload materialization
- tensor/control materialization
- sidecar/manifest realization
- generic indexing

It is one explicit broader replay-source carrier provenance/source-binding admission
contract-definition pass above the truthful blocked carrier-provenance/source-binding realization
result.

### Narrowest honest next boundary shape

The narrowest honest next boundary shape is:

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationExplicitCarrierProvenanceSourceBindingAdmissionContractV1`

### What it would consume

It would consume only:

- the admitted truthful blocked carrier-provenance/source-binding realization result
- the audited family root directory reference already preserved by that realization result

### What it would expose

It would expose exactly one contract-only broader replay-source-actual-materialization-facing
admission boundary per admitted specimen:

- one boundary keyed by the exact preserved receipt-bound lineage tuple, deferred observation
  lineage, preserved replay-input-locator handle kind, preserved replay-source-materialization
  requirement kind, preserved replay-source-actual-materialization handle kind, and preserved
  carrier-provenance/source-binding handle kind already carried by the truthful blocked
  carrier-provenance/source-binding realization result
- one explicit admission contract element whose only admitted meaning is:
  this exact preserved tuple may later receive one externally admitted explicit replay-source
  carrier provenance/source binding input, and the current lineage/root fields are not that input

### What remains deferred

This decision still defers:

- replay-source actual-materialization implementation
- replay-source carrier discovery
- replay-input locator logic
- replay file path materialization
- replay bytes materialization
- replay frames materialization
- actual `mimir_replay::ReplayInput`
- replay parsing
- raw-state payload materialization
- tensor materialization
- control/action extraction
- sidecar/manifest realization
- generic indexing
- `mimir_export`

### Why this is the minimum honest reopen

This is the minimum honest reopen because:

- the truthful blocked carrier-provenance/source-binding realization result is already explicit
  and machine-checkable
- the missing problem is now later than truthful blocked carrier-provenance/source-binding
  realization but earlier than replay parsing
- `source_replay` still remains opaque lineage only
- `source_replay.provenance_label` still remains opaque lineage only
- `audited_family_root_directory` still remains only a BC specimen-tree anchor
- no current evidence proves that sidecars, manifests, generic indexing, or `mimir_export`
  belong in the missing defect path

Anything narrower would only restate the already-known blocked truth. Anything broader would
invent replay-source carrier representation, locator, parser, or storage semantics that the
admitted result still does not carry.

## G. NEXT CONTRACT SHAPE V1

Because reopening is justified, the narrowest contract-only next boundary is fixed here.

### Contract name

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationExplicitCarrierProvenanceSourceBindingAdmissionContractV1`

### Exact inputs it would consume from the blocked carrier-provenance/source-binding realization result

At the top level, the contract would consume exactly:

- `specimen_count`
- `group_count`
- `source_contract_disposition`
- `source_contract_notes`
- `source_chosen_locator_contract_shape`
- `source_chosen_materialization_contract_shape`
- `source_chosen_replay_parsing_contract_shape`
- `source_chosen_replay_input_access_contract_shape`
- `source_chosen_replay_input_locator_contract_shape`
- `source_chosen_replay_input_locator_implementation_contract_shape`
- `source_chosen_replay_input_locator_actual_implementation_contract_shape`
- `source_chosen_replay_source_materialization_contract_shape`
- `source_chosen_replay_source_actual_materialization_contract_shape`
- `audited_family_root_directory`
- `realization_disposition`
- `realization_notes`
- `chosen_replay_source_actual_materialization_carrier_provenance_binding_contract_shape`

For one admitted specimen, the contract would consume exactly:

- `admitted_replay_source_actual_materialization_carrier_provenance_binding_boundary_input:
  LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationCarrierProvenanceBindingBoundaryInputV1`
- `admitted_replay_source_actual_materialization_carrier_provenance_binding_output_boundary:
  LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationCarrierProvenanceBindingOutputBoundaryV1`
- `admitted_replay_source_actual_materialization_carrier_provenance_binding_disposition:
  LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationCarrierProvenanceBindingRealizationDispositionV1`

Those values would be consumed only from:

- `consumed_replay_source_actual_materialization_carrier_provenance_binding_boundary_input`
- `preserved_replay_source_actual_materialization_carrier_provenance_binding_output_boundary`
- `replay_source_actual_materialization_carrier_provenance_binding_disposition`

The next contract would not recompute those values from lower layers.

### Exact broader replay-source-actual-materialization-facing boundary it would expose

The next contract would expose exactly one per-specimen broader
replay-source-actual-materialization-facing admission boundary made of:

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationExplicitCarrierProvenanceSourceBindingAdmissionBoundaryInputV1`
- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationExplicitCarrierProvenanceSourceBindingAdmissionOutputBoundaryV1`

The boundary input would preserve exactly:

- `lane_ordinal`
- `specimen_ordinal`
- `artifact_id`
- `anchored_bc_specimen_file_path`
- `source_raw_state_window_ref`
- `source_slice_id`
- `source_replay`
- `source_subject`
- `source_phase_id`
- `preserved_observation_binding_kind`
- `preserved_accepted_reference_window`
- `preserved_replay_input_locator_handle_kind`
- `preserved_replay_source_materialization_requirement_kind`
- `preserved_replay_source_actual_materialization_handle_kind`
- `preserved_replay_source_actual_materialization_carrier_provenance_binding_handle_kind`

The boundary output would preserve that exact same tuple and add only:

- `bound_explicit_replay_source_carrier_provenance_source_binding_admission_kind`

The exact new contract-only admission kind introduced here would be:

- `LowBoostRecoveryBcReceiptBoundReplaySourceActualMaterializationExplicitCarrierProvenanceSourceBindingAdmissionKindV1::FutureAdmittedExplicitReplaySourceCarrierProvenanceSourceBindingOnly`

Its admitted meaning would be only:

- this exact preserved tuple may later receive one externally admitted explicit replay-source
  carrier provenance/source binding input at a later realization boundary
- the current lineage tuple is not that admitted input
- the current audited-root presence is not that admitted input
- the contract does not say what the later carrier class is
- the contract does not say where that later carrier lives

### Exact invariant

The exact invariant is:

- for one admitted truthful blocked carrier-provenance/source-binding specimen, the tuple
  (`lane_ordinal`, `specimen_ordinal`, `artifact_id`, `anchored_bc_specimen_file_path`,
  `source_raw_state_window_ref`, `source_slice_id`, `source_replay`, `source_subject`,
  `source_phase_id`, `preserved_observation_binding_kind`,
  `preserved_accepted_reference_window`, `preserved_replay_input_locator_handle_kind`,
  `preserved_replay_source_materialization_requirement_kind`,
  `preserved_replay_source_actual_materialization_handle_kind`,
  `preserved_replay_source_actual_materialization_carrier_provenance_binding_handle_kind`) plus
  preserved deferred observation lineage and preserved receipt-bound lineage binds to exactly one
  honest broader replay-source-actual-materialization admission contract
- that contract is authoritative only for that exact preserved tuple
- that contract must not silently rewrite, drop, pad, reorder, or widen preserved lineage
- that contract must not reinterpret `source_replay` as replay source
- that contract must not reinterpret `source_replay.provenance_label` as a replay path contract
- that contract must not reinterpret `audited_family_root_directory` as replay storage
- that contract must not claim an admitted explicit carrier provenance/source binding already
  exists without a later boundary that actually receives one

### What it explicitly refuses to promise

The next contract explicitly refuses to promise:

- replay-source carrier discovery
- replay file path availability
- replay path derivation from `source_replay` or `source_replay.provenance_label`
- replay storage derivation from `audited_family_root_directory`
- replay bytes availability
- replay frames availability
- actual `mimir_replay::ReplayInput`
- replay parsing
- raw-state payload availability
- tensor availability
- control/action availability
- usefulness proof
- policy-improvement proof
- sidecar/manifest realization
- generic index semantics
- `mimir_export` integration

## H. ADMISSION RULES

A carrier-provenance/source-binding realization result may enter this reopen-decision boundary
only when all of the following hold:

1. the input is exactly
   `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationCarrierProvenanceBindingRealizationResultV1`
2. `source_contract_disposition ==`
   `ContractDefinedForReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationCarrierProvenanceBindingOnly`
3. `source_contract_notes` still equal the exact replay-source actual-materialization
   carrier-provenance/source-binding contract note set
4. `source_chosen_locator_contract_shape == ReceiptBoundSpecimenFileAnchored`
5. `source_chosen_materialization_contract_shape ==`
   `ReceiptBoundValidatedSpecimenFileSourceRawStateWindowRefMaterializationAttemptOnly`
6. `source_chosen_replay_parsing_contract_shape ==`
   `ReceiptBoundValidatedSpecimenFileSourceRawStateWindowRefReplaySideParseAttemptOnly`
7. `source_chosen_replay_input_access_contract_shape ==`
   `ReceiptBoundReplayInputAccessSourceBindingOnly`
8. `source_chosen_replay_input_locator_contract_shape == ReceiptBoundReplayInputLocatorOnly`
9. `source_chosen_replay_input_locator_implementation_contract_shape ==`
   `ReceiptBoundReplayInputLocatorImplementationOnly`
10. `source_chosen_replay_input_locator_actual_implementation_contract_shape ==`
    `ReceiptBoundReplayInputLocatorActualImplementationOnly`
11. `source_chosen_replay_source_materialization_contract_shape ==`
    `ReceiptBoundReplaySourceMaterializationOnly`
12. `source_chosen_replay_source_actual_materialization_contract_shape ==`
    `ReceiptBoundReplaySourceActualMaterializationOnly`
13. `chosen_replay_source_actual_materialization_carrier_provenance_binding_contract_shape ==`
    `ReceiptBoundReplaySourceActualMaterializationCarrierProvenanceBindingOnly`
14. `realization_disposition ==`
    `RealizedForTruthfulBlockedReplaySourceActualMaterializationCarrierProvenanceBindingOnly`
15. `realization_notes` still equal the exact truthful blocked carrier-provenance/source-binding
    realization note set
16. `specimen_count > 0`
17. `group_count > 0`
18. `group_count` equals the number of preserved ordered lane results
19. `audited_family_root_directory` still ends in `low_boost_recovery_bc_v1`
20. `audited_family_root_directory` still exists as a directory at decision time
21. lane order and specimen order still match concrete lane/specimen position
22. each consumed carrier-provenance/source-binding boundary input and each preserved
    carrier-provenance/source-binding output boundary still remain receipt-bound below
    `audited_family_root_directory`
23. each preserved carrier-provenance/source-binding output boundary still matches the
    corresponding consumed carrier-provenance/source-binding boundary input on:
    - `lane_ordinal`
    - `specimen_ordinal`
    - `artifact_id`
    - `anchored_bc_specimen_file_path`
    - `source_raw_state_window_ref`
    - `source_slice_id`
    - `source_replay`
    - `source_subject`
    - `source_phase_id`
    - `preserved_observation_binding_kind`
    - `preserved_accepted_reference_window`
    - `preserved_replay_input_locator_handle_kind`
    - `preserved_replay_source_materialization_requirement_kind`
    - `preserved_replay_source_actual_materialization_handle_kind`
24. each preserved output boundary still preserves
    `bound_replay_source_actual_materialization_carrier_provenance_binding_handle_kind ==`
    `FutureExplicitReplaySourceCarrierProvenanceBindingOnly`
25. each specimen still preserves
    `replay_source_actual_materialization_carrier_provenance_binding_disposition ==`
    `RealizedForTruthfulBlockedReplaySourceActualMaterializationCarrierProvenanceBindingOnly`
26. every preserved replay-input-locator handle kind still remains
    `FutureParserConsumableReplayHandleOnly`
27. every preserved replay-source-materialization requirement kind still remains
    `ExplicitReplaySourceMaterializationRequiredBeforeReplayInputOrParsing`
28. every preserved replay-source-actual-materialization handle kind still remains
    `FutureExplicitReplaySourceCarrierOnly`
29. no lower boundary is silently reopened to repair or reinterpret the admitted realization input

Admission here means only:

- this reopen decision may rely on the truthful blocked carrier-provenance/source-binding result
  as the last trusted pre-broader-reopen layer

Admission here does not mean:

- explicit replay-source carrier provenance/source binding already exists
- replay-source actual-materialization succeeds
- replay-input access succeeds
- replay parsing succeeds
- replay files, replay bytes, or replay frames are available
- raw-state payload exists
- tensors or controls are available

## I. FAILURE RULES

This pass must hard-fail for:

- degraded carrier-provenance/source-binding realization input
- degraded source-contract-note or realization-note sets
- count/order/root/path drift
- duplicate or missing artifact ids
- any input/output-boundary identity drift
- any specimen whose carrier-provenance/source-binding disposition no longer remains truthful
  blocked only
- any specimen whose replay-input-locator handle kind no longer remains
  `FutureParserConsumableReplayHandleOnly`
- any specimen whose replay-source-materialization requirement kind no longer remains
  `ExplicitReplaySourceMaterializationRequiredBeforeReplayInputOrParsing`
- any specimen whose replay-source-actual-materialization handle kind no longer remains
  `FutureExplicitReplaySourceCarrierOnly`
- any specimen whose carrier-provenance/source-binding handle kind no longer remains
  `FutureExplicitReplaySourceCarrierProvenanceBindingOnly`
- any attempt to reinterpret `source_replay` or `source_replay.provenance_label` as an implicit
  replay path or replay source
- any attempt to reinterpret the audited family root as replay storage
- any attempt to widen this pass into replay-source actual-materialization implementation
- any attempt to widen this pass into replay-source carrier discovery
- any attempt to widen this pass into replay-input locator logic
- any attempt to widen this pass into replay parsing
- any attempt to widen this pass into raw-state payload materialization
- any attempt to widen this pass into tensor/control materialization
- any attempt to widen this pass into sidecars, manifests, generic indexing, or `mimir_export`

This pass would produce `reopen not justified yet` only if all admission rules held and the
audited repo state either:

- already exposed an honest explicit carrier provenance/source binding admission boundary, or
- proved that sidecar/manifest/index reopening was the minimum honest next step

The audited repo state does not show either condition in v1.

So v1 behavior is:

- hard-fail on any admission violation
- otherwise emit the single reopen-justified decision defined above

## J. NON-GOALS

This pass does not do any of the following:

- no replay-source actual-materialization implementation
- no replay-source carrier discovery
- no replay-input locator logic
- no replay parsing
- no actual raw-state payload materialization
- no tensor materialization
- no control/action extraction
- no `mimir_export` integration
- no usefulness proof
- no policy-improvement proof
- no corpus-wide replay ingestion
- no replay rollout or physics work
- no async/background system
- no database work
- no deterministic-family reopen
- no execution-result cleanup boundary change

## K. RELATION TO NEXT STAGES

### What this pass now guarantees

This pass now guarantees:

- the broader replay-source actual-materialization reopen decision above the blocked
  carrier-provenance/source-binding realization is explicit and auditable
- the decision stays low-boost-recovery-specific
- the decision stays receipt-bound
- the narrowest honest next contract shape is fixed:
  `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationExplicitCarrierProvenanceSourceBindingAdmissionContractV1`
- `source_replay` remains opaque lineage only
- `source_replay.provenance_label` remains opaque lineage only
- `audited_family_root_directory` remains only a BC specimen-tree anchor
- `FutureParserConsumableReplayHandleOnly` still remains not replay source
- `ExplicitReplaySourceMaterializationRequiredBeforeReplayInputOrParsing` still remains a
  requirement-only boundary
- `FutureExplicitReplaySourceCarrierOnly` still remains not a realized explicit carrier
- `FutureExplicitReplaySourceCarrierProvenanceBindingOnly` still remains not a realized explicit
  carrier provenance/source binding
- `mimir_export` remains untouched and forbidden

### What remains deferred

This pass still does not guarantee:

- admitted explicit replay-source carrier provenance/source binding exists
- replay-source actual-materialization succeeds
- replay file path, replay bytes, or replay frames exist
- actual `mimir_replay::ReplayInput` exists
- replay parsing
- raw-state payload materialization
- tensor/control materialization
- sidecar/manifest necessity

### Immediate next-stage implication

The immediate next pass should be:

- one first narrow contract-definition pass for
  `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationExplicitCarrierProvenanceSourceBindingAdmissionContractV1`
- still above
  `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationCarrierProvenanceBindingRealizationResultV1`
- still without replay-source actual-materialization implementation
- still without replay-source carrier discovery
- still without replay-input locator logic
- still without replay parsing
- still without raw-state payload materialization
- still without tensor/control materialization
- still without `mimir_export` widening unless that separate boundary is explicitly reopened
