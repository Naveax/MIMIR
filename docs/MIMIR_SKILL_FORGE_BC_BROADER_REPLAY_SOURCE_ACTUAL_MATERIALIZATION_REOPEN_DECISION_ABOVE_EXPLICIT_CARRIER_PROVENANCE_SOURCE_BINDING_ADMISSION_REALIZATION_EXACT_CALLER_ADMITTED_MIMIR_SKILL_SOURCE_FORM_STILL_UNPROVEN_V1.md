# MIMIR Skill Forge BC Broader Replay-Source Actual-Materialization Reopen Decision Above Explicit Carrier Provenance/Source-Binding Admission Realization Exact Caller-Admitted `mimir-skill` Source Form Still Unproven v1

## A. PURPOSE

### What this pass owns

This pass owns exactly one narrower reopen-decision question above:

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationExplicitCarrierProvenanceSourceBindingAdmissionRealizationResultV1`

It decides only:

- whether the audited repo now justifies one exact caller-admitted source form inside a
  brand-new `mimir-skill` caller seam
- or whether another reopen-decision is still required because the repo can prove seam ownership
  but still cannot honestly freeze one exact caller-admitted source form

This pass is not:

- a contract-definition pass
- a realization pass
- replay-source actual-materialization implementation
- replay-source carrier discovery
- replay-input locator logic
- replay parsing
- raw-state payload materialization
- tensor/control materialization
- `mimir_export` widening

### Why it exists

The prior audited reopen decision already fixed one important fact:

- the only remaining honest source-form class is a brand-new caller-supplied surface in
  `mimir-skill`

What remained unresolved was narrower:

- whether the repo now proves one exact caller-admitted source form inside that new seam
- and, only if it does, whether one exact next contract-definition boundary can be named

This pass answers that exact narrower question using the current docs, current public crate
surfaces, and the current crate dependency edges.

## B. FAMILY SCOPE

`low_boost_recovery` is the only supported family in this reopen-decision version.

This decision remains family-specific because:

- the admitted input remains only
  `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationExplicitCarrierProvenanceSourceBindingAdmissionRealizationResultV1`
- the preserved lineage tuple remains only the low-boost-recovery receipt-bound BC specimen tuple
- the blocked explicit-admission chain still lives only in `mimir-skill`
- no second family exists that would justify a shared replay/raw-state/index/export/materialization
  caller-admission framework

No generic multi-family replay/raw-state/index/export/materialization framework is introduced
here.

## C. INPUT BOUNDARY

This pass consumes only:

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationExplicitCarrierProvenanceSourceBindingAdmissionRealizationResultV1`
- the audited family root directory reference already preserved by that result
- `docs/ARTIFACT_VERSIONING.md`
- `docs/DATA_CONTRACTS.md`
- `docs/MIMIR_SKILL_FORGE_BC_ACTUAL_FILESYSTEM_EMISSION_V1.md`
- `docs/MIMIR_SKILL_FORGE_BC_EMITTED_OUTPUT_REFINEMENT_SIDECAR_DECISION_V1.md`
- `executor_mimir_skill_forge_v1_staged_delivery_rules.txt`
- the current public crate surfaces in `mimir-skill`, `mimir-replay`, `mimir-io`,
  `mimir-export`, and `mimir-types`
- the current `Cargo.toml` dependency edges for those same crates

From the blocked explicit-admission realization result, this pass consumes exactly:

- `specimen_count`
- `group_count`
- `source_contract_disposition`
- `source_contract_notes`
- `source_chosen_locator_contract_shape`
- `source_chosen_materialization_contract_shape`
- `source_chosen_replay_parsing_contract_shape`
- `source_chosen_replay_input_access_contract_shape`
- `source_chosen_replay_input_locator_contract_shape`
- `source_chosen_replay_input_locator_implementation_contract_shape`
- `source_chosen_replay_input_locator_actual_implementation_contract_shape`
- `source_chosen_replay_source_materialization_contract_shape`
- `source_chosen_replay_source_actual_materialization_contract_shape`
- `source_chosen_replay_source_actual_materialization_carrier_provenance_binding_contract_shape`
- `audited_family_root_directory`
- ordered `preserved_ordered_lane_results`
- each `lane_ordinal`
- each ordered `ordered_specimen_results`
- each `specimen_ordinal`
- each admitted explicit-admission boundary input/output pair
- each blocked explicit-admission realization disposition
- `realization_disposition`
- `realization_notes`
- `chosen_replay_source_actual_materialization_explicit_carrier_provenance_source_binding_admission_contract_shape`

Lower contract/reopen/realization layers are no longer direct input here.

## D. AUDITED SURFACE FINDINGS

The audited repo now proves all of the following:

1. Storage-based source-form classes remain closed by unchanged audited boundaries.
   - `docs/ARTIFACT_VERSIONING.md` still limits explicit versioning to top-level persisted
     artifacts.
   - `docs/MIMIR_SKILL_FORGE_BC_ACTUAL_FILESYSTEM_EMISSION_V1.md` still freezes one family root,
     one lane directory per lane, and one specimen JSON file per specimen.
   - `docs/MIMIR_SKILL_FORGE_BC_EMITTED_OUTPUT_REFINEMENT_SIDECAR_DECISION_V1.md` still keeps
     sidecar/manifest semantics unjustified.
   - `executor_mimir_skill_forge_v1_staged_delivery_rules.txt` still forbids widening replay,
     export-bundle, or orchestration boundaries during v1 spec follow-through unless explicitly
     reopened.
2. `mimir-skill` is still the only honest ownership location for any new family-specific caller
   seam on this defect path.
   - The blocked explicit-admission chain lives there.
   - The highest public surfaces on this path still stop at:
     - `define_low_boost_recovery_bc_receipt_bound_validated_specimen_file_raw_state_window_replay_source_actual_materialization_explicit_carrier_provenance_source_binding_admission_contract_v1(...)`
     - `realize_low_boost_recovery_bc_receipt_bound_validated_specimen_file_raw_state_window_replay_source_actual_materialization_explicit_carrier_provenance_source_binding_admission_v1(...)`
   - `mimir-skill` already accepts one family-specific caller-provided filesystem input via
     `emit_low_boost_recovery_bc_actual_filesystem_emission_v1(..., output_parent_directory)`.
3. `mimir-types::ReplaySourceRef` still contains only:
   - `replay_id`
   - `provenance_label`
   It remains opaque lineage only.
4. `mimir-types::LowBoostRecoveryBcSerializedArtifactV1` and
   `mimir-types::LowBoostRecoveryBcActualFilesystemEmissionSpecimenFileV1` still preserve only
   receipt-bound lineage plus `source_replay: ReplaySourceRef`.
   They still do not carry:
   - replay path
   - replay bytes
   - replay frames
   - carrier class
   - explicit caller-admitted source binding
5. `mimir-io` still exposes only raw read/write helpers for persisted artifacts and the emitted
   low-boost-recovery specimen file.
   It still does not expose:
   - replay-source binding readers
   - caller-binding loaders
   - sibling-sidecar readers
   - family-specific manifest readers
6. `mimir-replay` still exposes only:
   - `ReplayInput::File(PathBuf)`
   - `ReplayInput::Memory { label, bytes }`
   - `ReplayHeader`
   - `ReplayReader`
   - `UnsupportedReplayReader`
   Workspace audit still shows no `ReplayInput` use outside `mimir-replay`.
7. `mimir-skill` does not currently depend on `mimir-replay`.
   Its `Cargo.toml` still depends only on:
   - `mimir-core`
   - `mimir-io`
   - `mimir-types`
   - `serde`
   So a `ReplayInput`-backed caller seam is not an already-established dependency edge.
8. The current public note surfaces still keep multiple future carrier classes open rather than
   freezing one exact caller-admitted form.
   - The replay-source actual-materialization carrier-provenance binding contract notes still
     preserve:
     `FutureFileBackedByteBackedReplayFrameBackedOrMimirReplayReplayInputTargetsMayBeAcknowledgedWithoutMaterializationOrPromise`
   - The explicit carrier provenance/source-binding admission contract notes still preserve:
     `FutureExternallyAdmittedExplicitCarrierProvenanceSourceBindingMayBeAcknowledgedWithoutCarrierClassLocatorStorageBytesFramesOrMimirReplayReplayInputPromise`
   Those notes explicitly refuse to freeze one exact caller-admitted source form.
9. `mimir-export` remains unrelated export/ledger orchestration on this defect path and widening
   into it remains forbidden.

## E. EXACT CALLER-ADMITTED SOURCE-FORM AUDIT

The exact question in this pass is:

- does the audited repo justify one exact caller-admitted source form inside a brand-new
  `mimir-skill` caller seam

### Exact form candidate 1: file-path-backed caller admission

This is not justified now.

Why:

- no admitted replay path exists above the blocked explicit-admission realization
- `source_replay` and `source_replay.provenance_label` remain opaque lineage only
- `audited_family_root_directory` remains only a BC specimen-tree anchor
- choosing a path-backed form would invent replay locator/storage semantics

### Exact form candidate 2: byte-backed caller admission

This is not justified now.

Why:

- no current DTO on this defect path carries replay bytes
- no current caller seam or loader surface binds bytes to the preserved receipt-bound tuple
- choosing bytes now would invent storage/materialization semantics

### Exact form candidate 3: replay-frame-backed caller admission

This is not justified now.

Why:

- replay-frame-backed admission appears only as a future-acknowledged target class in current
  `mimir-skill` notes
- no public frame carrier type exists on this defect path
- no current caller seam or consumer binds replay frames to the preserved tuple
- choosing this now would invent frame semantics

### Exact form candidate 4: `mimir_replay::ReplayInput`-backed caller admission

This is not justified now.

Why:

- `ReplayInput` exists only in `mimir-replay`
- workspace audit still shows no `ReplayInput` use outside `mimir-replay`
- `mimir-skill` does not currently depend on `mimir-replay`
- `ReplayInput` does not by itself carry receipt-bound tuple identity or
  carrier-provenance/source-binding semantics
- treating `ReplayInput` existence alone as proof is explicitly forbidden in this pass

### Exact form candidate 5: pure provenance-only caller admission

This is not justified now.

Why:

- no public DTO above the blocked realization defines a caller-admitted provenance-only payload
- `ReplaySourceRef` is still only opaque lineage, not a caller-admitted explicit source-binding
  contract
- freezing a provenance-only form now would invent the exact binding payload semantics and the
  exact relationship between provenance and future carrier realization

### Narrowest truthful answer in v1

The audited repo now proves:

- the ownership location of the future caller seam is `mimir-skill`

But the audited repo still does not prove:

- one exact caller-admitted source form inside that seam
- one exact next contract-definition boundary name above the blocked realization

## F. DECISION

The decision in v1 is:

- Outcome A: another reopen-decision is still required because the caller-supplied
  `mimir-skill` seam does not yet justify one exact caller-admitted source form

### Why Outcome A is the honest choice

Outcome B is not honest yet because naming one exact contract-definition boundary now would still
force the repo to invent at least one unproven semantic:

- exact caller payload shape
- exact carrier class
- exact locator/storage semantics
- exact provenance-to-carrier binding semantics

Outcome C is too weak because this pass does produce a narrower audited answer than the previous
caller-seam pass:

- the seam ownership location remains proven as `mimir-skill`
- file-path, bytes, replay-frame, `ReplayInput`, and provenance-only exact forms are each still
  unproven
- the next missing problem is now specifically another reopen-decision over exact caller-admitted
  form, not a broader source-form-class question

### Why one exact next contract-definition boundary still cannot be named

The repo still does not justify:

- one exact caller-admitted object shape
- one exact field set for that object
- one exact admitted carrier/provenance relationship

So one exact next contract-definition boundary name would still be invented rather than audited.

## G. WHY ARTIFACT / SIBLING-SIDECAR / MANIFEST-INDEX CANDIDATES REMAIN CLOSED

These classes remain closed because no new audited evidence reopens them:

- new family-specific artifact remains closed because `docs/ARTIFACT_VERSIONING.md` still limits
  top-level artifact versioning and this defect path still exposes no replay-source admission
  artifact producer or reader
- family-specific sibling-sidecar remains closed because
  `docs/MIMIR_SKILL_FORGE_BC_ACTUAL_FILESYSTEM_EMISSION_V1.md` still freezes exactly one emitted
  specimen JSON file per specimen and the emitted-output refinement sidecar decision remains
  unchanged
- manifest/index remains closed because the staged-delivery rules still forbid widening replay or
  export orchestration here, and `mimir-export` manifests remain unrelated orchestration

This pass does not reopen those closed classes.

## H. WHY PARSER WORK IS STILL DOWNSTREAM AND FORBIDDEN

Replay parsing remains downstream and forbidden because the repo still lacks one exact admitted
caller source form above the blocked explicit-admission realization.

That means the repo still lacks every parser prerequisite that would be needed here:

- no admitted replay path
- no admitted replay bytes
- no admitted replay frames
- no admitted `mimir_replay::ReplayInput`
- no admitted provenance-only binding DTO

So this pass stays strictly below:

- replay-source actual-materialization implementation
- replay-source carrier discovery
- replay-input locator logic
- replay parsing
- raw-state payload materialization
- tensor/control materialization

## I. WHY `source_replay`, `source_replay.provenance_label`, AND THE AUDITED ROOT STILL CANNOT SERVE

`source_replay` still cannot serve because it remains only:

- `ReplaySourceRef { replay_id, provenance_label }`

That is still lineage only, not an explicit caller-admitted source contract.

`source_replay.provenance_label` still cannot serve because no audited surface binds it to:

- a replay path
- replay bytes
- replay frames
- a caller-admitted provenance/source-binding DTO
- an admitted `mimir_replay::ReplayInput`

`audited_family_root_directory` still cannot serve because the repo still uses it only as:

- a BC specimen-tree anchor

No audited surface binds it to replay storage or to an explicit caller-admitted source.

## J. ADMISSION RULES

The blocked explicit-admission realization may enter this reopen-decision boundary only when all
of the following hold:

1. the input remains exactly
   `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationExplicitCarrierProvenanceSourceBindingAdmissionRealizationResultV1`
2. `realization_disposition ==`
   `RealizedForTruthfulBlockedReplaySourceActualMaterializationExplicitCarrierProvenanceSourceBindingAdmissionOnly`
3. the preserved chosen shapes remain the exact frozen receipt-bound shapes already required below
4. `audited_family_root_directory` still exists as a directory and still ends in
   `low_boost_recovery_bc_v1`
5. every preserved explicit admission kind still remains
   `FutureAdmittedExplicitReplaySourceCarrierProvenanceSourceBindingOnly`
6. the audited doc, crate-surface, and dependency-edge findings listed in this pass still hold

Admission here means only:

- this reopen decision may treat the blocked explicit-admission realization as the last trusted
  pre-caller-source-form layer

Admission here does not mean:

- an exact caller-admitted source form already exists
- explicit replay-source carrier provenance/source binding already exists
- replay-source actual-materialization succeeds
- replay parsing succeeds

## K. FAILURE RULES

This pass must hard-fail for:

- degraded blocked explicit-admission realization input
- degraded contract-note or realization-note sets
- count/order/root/path drift
- duplicate or missing artifact ids
- any attempt to reopen artifact, sibling-sidecar, or manifest/index classes without new audited
  evidence
- any attempt to treat `ReplayInput` existing in `mimir-replay` as proof of one exact
  caller-admitted source form
- any attempt to reinterpret `source_replay` or `source_replay.provenance_label` as an implicit
  replay path or caller source
- any attempt to reinterpret `audited_family_root_directory` as replay storage
- any attempt to widen this pass into replay-source actual-materialization implementation
- any attempt to widen this pass into replay-source carrier discovery
- any attempt to widen this pass into replay-input locator logic
- any attempt to widen this pass into replay parsing
- any attempt to widen this pass into raw-state payload materialization
- any attempt to widen this pass into tensor/control materialization
- any attempt to widen this pass into `mimir_export`
- any attempt to add a new Rust caller DTO before one exact source form is justified

## L. RELATION TO NEXT STAGES

### What this pass now guarantees

This pass now guarantees:

- the only honest ownership location for any future caller-admission seam remains `mimir-skill`
- artifact, sibling-sidecar, and manifest/index classes remain closed unless new audited evidence
  appears
- the repo still does not justify one exact caller-admitted source form
- the repo still does not justify one exact next contract-definition boundary name
- replay parsing remains downstream and forbidden
- `source_replay` remains opaque lineage only
- `source_replay.provenance_label` remains opaque lineage only
- `audited_family_root_directory` remains only a BC specimen-tree anchor
- `mimir_export` remains untouched and forbidden

### What remains deferred

This pass still does not guarantee:

- one exact caller-admitted source form
- one exact next contract-definition boundary
- explicit replay-source carrier provenance/source binding input
- replay-source actual-materialization success
- replay-source carrier discovery success
- replay-input locator logic
- replay parsing
- replay bytes or replay frames
- actual `mimir_replay::ReplayInput`
- raw-state payload materialization
- tensor/control materialization

### Immediate next-stage implication

The immediate next pass should be:

- another reopen-decision pass above
  `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceActualMaterializationExplicitCarrierProvenanceSourceBindingAdmissionRealizationResultV1`
- focused only on whether one exact caller-admitted source form can now be proven without
  inventing payload, locator, or carrier semantics
- still low-boost-recovery-specific
- still receipt-bound
- still without `mimir_export` widening unless explicitly reopened
