# MIMIR Skill Forge BC Replay-Source Materialization Reopen Decision v1

## A. PURPOSE

### What this pass owns

This pass owns exactly one broader reopen-decision question above:

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorActualImplementationRealizationResultV1`

It defines:

- whether one receipt-bound replay-source materialization boundary must now be deliberately reopened
  at all
- the exact criteria used to answer that question
- the narrowest honest next contract shape only if reopening is justified

### Why it exists

The boundary below this pass already fixed the current truthful limit:

- `RealizedForTruthfulBlockedReplayInputLocatorActualImplementationOnly`

That matters because the repo can now prove all of the following:

- the admitted low-boost-recovery specimen identity and deferred observation lineage survive into
  the truthful blocked actual-implementation result
- the blocked result still exposes no replay file path
- the blocked result still exposes no replay bytes
- the blocked result still exposes no replay frames
- the blocked result still exposes no actual `mimir_replay::ReplayInput`
- `source_replay` still remains opaque lineage only
- `source_replay.provenance_label` still remains opaque lineage only
- `audited_family_root_directory` still remains only a BC specimen-tree anchor

This pass exists because the repo can now prove it cannot honestly materialize replay source from
the currently admitted contract/result chain without reopening one explicit boundary first.

### How it differs from the actual-implementation realization boundary below it

- The lower boundary realizes the current truth:
  `RealizedForTruthfulBlockedReplayInputLocatorActualImplementationOnly`.
- This pass does not realize more output.
- This pass decides whether one broader replay-source materialization boundary must now be
  deliberately reopened above that truthful blocked result.
- This pass is still not replay-input locator logic.
- This pass is still not replay parsing.
- This pass is still not raw-state payload materialization.

## B. FAMILY SCOPE

`low_boost_recovery` is the only supported family in this reopen-decision version.

This replay-source-materialization reopen decision remains family-specific because:

- the admitted input is only
  `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorActualImplementationRealizationResultV1`
- the preserved lineage tuple is only the low-boost-recovery BC specimen tuple already carried by
  that result
- the preserved deferred observation lineage is only the low-boost-recovery BC observation-binding
  pair already carried by that result
- no second family exists that would justify a shared replay/raw-state/index/export/materialization
  framework

No generic all-family replay/raw-state/index/export/materialization framework is introduced here.

## C. INPUT BOUNDARY

This pass consumes only:

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorActualImplementationRealizationResultV1`
- the audited family root directory reference already preserved by that result

From
`LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorActualImplementationRealizationResultV1`,
this pass consumes exactly:

- `specimen_count`
- `group_count`
- `source_contract_disposition`
- `source_contract_notes`
- `source_chosen_locator_contract_shape`
- `source_chosen_materialization_contract_shape`
- `source_chosen_replay_parsing_contract_shape`
- `source_chosen_replay_input_access_contract_shape`
- `source_chosen_replay_input_locator_contract_shape`
- `source_chosen_replay_input_locator_implementation_contract_shape`
- `audited_family_root_directory`
- ordered `preserved_ordered_lane_results`
- each `lane_ordinal`
- each ordered `ordered_specimen_results`
- each `specimen_ordinal`
- each `consumed_replay_input_locator_actual_implementation_boundary_input.lane_ordinal`
- each `consumed_replay_input_locator_actual_implementation_boundary_input.specimen_ordinal`
- each `consumed_replay_input_locator_actual_implementation_boundary_input.artifact_id`
- each
  `consumed_replay_input_locator_actual_implementation_boundary_input.anchored_bc_specimen_file_path`
- each
  `consumed_replay_input_locator_actual_implementation_boundary_input.source_raw_state_window_ref`
- each `consumed_replay_input_locator_actual_implementation_boundary_input.source_slice_id`
- each `consumed_replay_input_locator_actual_implementation_boundary_input.source_replay`
- each `consumed_replay_input_locator_actual_implementation_boundary_input.source_subject`
- each `consumed_replay_input_locator_actual_implementation_boundary_input.source_phase_id`
- each
  `consumed_replay_input_locator_actual_implementation_boundary_input.preserved_observation_binding_kind`
- each
  `consumed_replay_input_locator_actual_implementation_boundary_input.preserved_accepted_reference_window`
- each
  `preserved_replay_input_locator_actual_implementation_output_boundary.lane_ordinal`
- each
  `preserved_replay_input_locator_actual_implementation_output_boundary.specimen_ordinal`
- each `preserved_replay_input_locator_actual_implementation_output_boundary.artifact_id`
- each
  `preserved_replay_input_locator_actual_implementation_output_boundary.anchored_bc_specimen_file_path`
- each
  `preserved_replay_input_locator_actual_implementation_output_boundary.source_raw_state_window_ref`
- each `preserved_replay_input_locator_actual_implementation_output_boundary.source_slice_id`
- each `preserved_replay_input_locator_actual_implementation_output_boundary.source_replay`
- each `preserved_replay_input_locator_actual_implementation_output_boundary.source_subject`
- each `preserved_replay_input_locator_actual_implementation_output_boundary.source_phase_id`
- each
  `preserved_replay_input_locator_actual_implementation_output_boundary.preserved_observation_binding_kind`
- each
  `preserved_replay_input_locator_actual_implementation_output_boundary.preserved_accepted_reference_window`
- each
  `preserved_replay_input_locator_actual_implementation_output_boundary.bound_replay_input_locator_handle_kind`
- each `replay_input_locator_actual_implementation_disposition`
- `realization_disposition`
- `realization_notes`
- `chosen_replay_input_locator_actual_implementation_contract_shape`

The following are no longer direct input at this boundary:

- replay-input locator actual-implementation contracts below this layer
- replay-input locator implementation contracts below this layer
- replay-input locator implementation reopen decisions below this layer
- replay-input locator realizations below this layer
- replay-input-access/source-binding realizations below this layer
- blocked replay-side parse-attempt results
- parse-attempt contracts
- materialization-attempt realizations
- replay-parsing reopen decisions
- replay-parsing-success reopen decisions
- materialization-contract results
- validation results
- lower locator-contract results
- lower planning boundaries
- emitted-output audit/readback results
- actual emission receipts
- persisted artifacts
- BC rows
- `mimir_export`

Those lower layers remain frozen. This pass starts strictly from the truthful blocked
actual-implementation realization result and the already-preserved audited family root reference.

## D. DECISION QUESTION

The exact question is:

- should one broader replay-source materialization boundary now be deliberately reopened above the
  truthful blocked actual-implementation realization result?

This question exists only because the current repo can now prove something narrower and stricter
than before:

- the current admitted contract/result chain can preserve receipt-bound identity and deferred
  observation lineage
- the same chain cannot honestly yield replay source semantics from the currently admitted fields
  without cheating through implicit path or storage reinterpretation

That means the unresolved gap is no longer:

- whether the blocked actual-implementation result is explicit
- whether the repo already has a replay-input locator actual-implementation boundary

The unresolved gap is earlier than replay parsing but later than the truthful blocked
actual-implementation result:

- whether the repo must now reopen one explicit replay-source materialization boundary so later
  work can admit explicit replay-source semantics without inventing them from opaque lineage

## E. REOPEN CRITERIA

Reopening is justified only if all of the following hold:

1. the remaining missing piece is truly replay-source materialization and not another defect in
   the lower replay-input locator chain
2. the next boundary can stay low-boost-recovery-specific
3. the next boundary can stay receipt-bound
4. the next boundary can avoid `mimir_export`
5. the next boundary can stay below replay parsing
6. the next boundary can stay below raw-state payload materialization, tensor materialization, and
   control/action materialization
7. the next boundary can introduce one explicit replay-source semantics boundary without cheating
   through `source_replay`, `source_replay.provenance_label`, or `audited_family_root_directory`
   reinterpretation
8. sidecars, manifests, and generic indexing remain unjustified unless the audited repo state now
   proves one of them is the minimum honest way to represent replay source
9. the audited crate surfaces do not already supply a richer truthful replay-source boundary that
   would make this reopen dishonest or redundant:
   - `mimir-skill` already makes the blocked actual-implementation result machine-checkable
   - `mimir-replay` still exposes only `ReplayInput`, `ReplayHeader`, `ReplayReader`, and
     `UnsupportedReplayReader`
   - `mimir-io` still exposes raw artifact read/write helpers and no replay-source locator or
     materialization framework
   - `mimir-export` still remains unrelated and untouched
   - `mimir-types::ReplaySourceRef` still contains only `replay_id` and `provenance_label`
10. the broader reopen still remains narrower than replay parsing, raw-state payload
    materialization, tensor/control materialization, corpus-wide replay ingestion, sidecar
    realization, and generic framework work

The audited repo state in v1 satisfies those criteria.

## F. DECISION

The decision in v1 is:

- reopen justified for one narrow receipt-bound replay-source materialization boundary

### Why reopening is justified

Reopening is justified because the truthful blocked actual-implementation result already isolates
the missing piece precisely:

- the repo preserves enough trustworthy receipt-bound identity and deferred observation lineage to
  bind one later explicit replay-source boundary
- the repo still has no honest way to move from that preserved tuple to explicit replay-source
  semantics without violating the opacity/root rules
- replay parsing still sits downstream of that missing source boundary

The audited repo state also shows that the missing boundary is not already implemented elsewhere:

- `mimir-replay` exposes a replay-input type, but the current admitted result cannot truthfully
  produce one
- `mimir-io` does not provide a replay-source lookup or materialization layer
- `mimir-export` is unrelated to the missing defect path and remains forbidden

That means the minimum honest reopen is not:

- another lower replay-input locator-chain repair
- replay parsing
- raw-state payload materialization
- tensor/control materialization
- sidecar/manifest realization
- generic indexing

It is one explicit replay-source materialization contract-definition pass above the truthful
blocked actual-implementation result.

### Narrowest honest next boundary shape

The narrowest honest next boundary shape is:

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceMaterializationContractV1`

### What it would consume

It would consume only:

- the admitted truthful blocked actual-implementation realization result
- the audited family root directory reference already preserved by that realization result

### What it would expose

It would expose exactly one contract-only replay-source-materialization-facing boundary per
admitted specimen:

- one boundary keyed by the exact preserved receipt-bound lineage tuple and deferred observation
  lineage already carried by the truthful blocked actual-implementation result
- one explicit replay-source-materialization-facing contract element whose only admitted meaning is
  that this exact preserved tuple now requires a later explicit replay-source materialization step
  before replay-input, replay-path, replay-byte, or replay-parsing claims can exist

### What remains deferred

This decision still defers:

- replay-input locator logic
- replay file discovery
- replay file path materialization
- replay bytes materialization
- replay frames materialization
- actual `mimir_replay::ReplayInput`
- replay parsing
- raw-state payload materialization
- tensor materialization
- control/action extraction
- sidecar/manifest realization
- generic indexing
- `mimir_export`

### Why this is the minimum honest reopen

This is the minimum honest reopen because:

- the truthful blocked actual-implementation result is already explicit and machine-checkable
- the missing problem is now later than blocked actual-implementation realization but earlier than
  replay parsing
- `source_replay` still remains opaque lineage only
- `source_replay.provenance_label` still remains opaque lineage only
- `audited_family_root_directory` still remains only a BC specimen-tree anchor
- no current evidence proves that sidecars, manifests, generic indexing, or `mimir_export` belong
  in the missing defect path

Anything narrower would only restate the already-known blocked truth. Anything broader would
invent replay-source semantics, parser semantics, or framework scope that the admitted result
still does not carry.

## G. REPLAY-SOURCE MATERIALIZATION CONTRACT SHAPE V1

Because reopening is justified, the narrowest contract-only next boundary is fixed here.

### Contract name

- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceMaterializationContractV1`

### Exact inputs it would consume from the blocked actual-implementation realization result

At the top level, the contract would consume exactly:

- `specimen_count`
- `group_count`
- `source_contract_disposition`
- `source_contract_notes`
- `source_chosen_locator_contract_shape`
- `source_chosen_materialization_contract_shape`
- `source_chosen_replay_parsing_contract_shape`
- `source_chosen_replay_input_access_contract_shape`
- `source_chosen_replay_input_locator_contract_shape`
- `source_chosen_replay_input_locator_implementation_contract_shape`
- `audited_family_root_directory`
- `realization_disposition`
- `realization_notes`
- `chosen_replay_input_locator_actual_implementation_contract_shape`

For one admitted specimen, the contract would consume exactly:

- `lane_ordinal`
- `specimen_ordinal`
- `artifact_id`
- `anchored_bc_specimen_file_path`
- `source_raw_state_window_ref`
- `source_slice_id`
- `source_replay`
- `source_subject`
- `source_phase_id`
- `preserved_observation_binding_kind`
- `preserved_accepted_reference_window`
- `bound_replay_input_locator_handle_kind`
- `replay_input_locator_actual_implementation_disposition`

Those values would be consumed only from:

- `consumed_replay_input_locator_actual_implementation_boundary_input`
- `preserved_replay_input_locator_actual_implementation_output_boundary`
- `replay_input_locator_actual_implementation_disposition`

The next contract would not recompute those values from lower layers.

### Exact replay-source-materialization-facing boundary it would expose

The next contract would expose exactly one per-specimen replay-source-materialization-facing
boundary that:

- preserves the exact admitted receipt-bound lineage tuple
- preserves the exact deferred observation lineage fields
- preserves that the current replay-input-locator handle kind is still
  `FutureParserConsumableReplayHandleOnly`
- adds only one new explicit replay-source-materialization-facing contract element whose admitted
  meaning is:
  this exact preserved tuple may later enter one explicit replay-source materialization step
  without deriving replay source from opaque lineage or audited-root presence alone

That next contract may acknowledge that later work could eventually target file-backed,
byte-backed, or `mimir_replay::ReplayInput`-backed semantics, but it must not materialize or
promise any of them in the contract itself.

### Exact invariant

The exact invariant is:

- for one admitted low-boost-recovery truthful blocked actual-implementation specimen, the tuple
  (`lane_ordinal`, `specimen_ordinal`, `artifact_id`, `anchored_bc_specimen_file_path`,
  `source_raw_state_window_ref`, `source_slice_id`, `source_replay`, `source_subject`,
  `source_phase_id`, `preserved_observation_binding_kind`,
  `preserved_accepted_reference_window`) plus preserved deferred observation lineage, preserved
  receipt-bound lineage, and the preserved `FutureParserConsumableReplayHandleOnly` contract-only
  handle kind binds to exactly one honest replay-source-materialization-facing contract
- that contract is authoritative only for that exact preserved tuple
- that contract must not silently rewrite, drop, pad, reorder, or widen preserved lineage
- that contract must not reinterpret `source_replay.provenance_label` as a path contract
- that contract must not reinterpret `audited_family_root_directory` as replay storage

### What it explicitly refuses to promise

The next contract explicitly refuses to promise:

- replay file discovery
- replay file path availability
- replay bytes availability
- replay frames availability
- actual `mimir_replay::ReplayInput`
- replay parsing
- raw-state payload availability
- tensor availability
- control/action availability
- usefulness proof
- policy-improvement proof
- sidecar/manifest realization
- generic index semantics
- `mimir_export` integration

## H. ADMISSION RULES

An actual-implementation realization result may enter this reopen-decision boundary only when all
of the following hold:

1. the input is exactly
   `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorActualImplementationRealizationResultV1`
2. `source_contract_disposition ==`
   `ContractDefinedForReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorActualImplementationOnly`
3. `source_contract_notes` still equal the exact replay-input locator actual-implementation
   contract note set
4. `source_chosen_locator_contract_shape == ReceiptBoundSpecimenFileAnchored`
5. `source_chosen_materialization_contract_shape ==`
   `ReceiptBoundValidatedSpecimenFileSourceRawStateWindowRefMaterializationAttemptOnly`
6. `source_chosen_replay_parsing_contract_shape ==`
   `ReceiptBoundValidatedSpecimenFileSourceRawStateWindowRefReplaySideParseAttemptOnly`
7. `source_chosen_replay_input_access_contract_shape ==`
   `ReceiptBoundReplayInputAccessSourceBindingOnly`
8. `source_chosen_replay_input_locator_contract_shape == ReceiptBoundReplayInputLocatorOnly`
9. `source_chosen_replay_input_locator_implementation_contract_shape ==`
   `ReceiptBoundReplayInputLocatorImplementationOnly`
10. `realization_disposition ==`
    `RealizedForTruthfulBlockedReplayInputLocatorActualImplementationOnly`
11. `realization_notes` still equal the exact truthful blocked actual-implementation realization
    note set
12. `chosen_replay_input_locator_actual_implementation_contract_shape ==`
    `ReceiptBoundReplayInputLocatorActualImplementationOnly`
13. `specimen_count > 0`
14. `group_count > 0`
15. `group_count` equals the number of preserved ordered lane results
16. `audited_family_root_directory` still ends in `low_boost_recovery_bc_v1`
17. `audited_family_root_directory` still exists as a directory at decision time
18. lane order and specimen order still match concrete lane/specimen position
19. each
    `consumed_replay_input_locator_actual_implementation_boundary_input.anchored_bc_specimen_file_path`
    still remains receipt-bound below `audited_family_root_directory`
20. each `preserved_replay_input_locator_actual_implementation_output_boundary` still matches the
    corresponding consumed actual-implementation boundary input on:
    - `lane_ordinal`
    - `specimen_ordinal`
    - `artifact_id`
    - `anchored_bc_specimen_file_path`
    - `source_raw_state_window_ref`
    - `source_slice_id`
    - `source_replay`
    - `source_subject`
    - `source_phase_id`
    - `preserved_observation_binding_kind`
    - `preserved_accepted_reference_window`
21. each specimen still preserves
    `replay_input_locator_actual_implementation_disposition ==`
    `RealizedForTruthfulBlockedReplayInputLocatorActualImplementationOnly`
22. each preserved output boundary still preserves
    `bound_replay_input_locator_handle_kind ==`
    `FutureParserConsumableReplayHandleOnly`
23. no lower boundary is silently reopened to repair or reinterpret the admitted realization input

Admission here means only:

- this reopen decision may rely on the truthful blocked actual-implementation result as the last
  trusted pre-replay-source-materialization layer

Admission here does not mean:

- replay source is materialized
- replay-input locator logic is implemented
- replay-input location succeeds
- replay parsing succeeds
- replay files, replay bytes, or replay frames are available
- raw-state payload exists
- tensors or controls are available

## I. FAILURE RULES

This pass must hard-fail for:

- degraded actual-implementation realization input
- count/order/root/path drift
- duplicate or missing artifact ids
- any input/output-boundary identity drift
- any specimen whose actual-implementation disposition no longer remains truthful blocked only
- any attempt to reinterpret `source_replay` or `provenance_label` as an implicit replay path
- any attempt to reinterpret the audited family root as replay storage
- any attempt to widen this pass into replay-input locator logic
- any attempt to widen this pass into replay parsing
- any attempt to widen this pass into raw-state payload materialization
- any attempt to widen this pass into tensor/control materialization
- any attempt to widen this pass into sidecars, manifests, generic indexing, or `mimir_export`

This v1 pass does not emit a separate valid-input `reopen not justified yet` disposition.

Reason:

- once the admitted truthful blocked actual-implementation result remains exact, the remaining
  missing boundary is already the replay-source-materialization question owned here

So failure behavior is:

- hard-fail on any admission violation
- otherwise emit the single reopen-justified decision defined above

## J. NON-GOALS

This pass does not do any of the following:

- no replay-input locator logic
- no replay parsing
- no actual raw-state payload materialization
- no tensor materialization
- no control/action extraction
- no `mimir_export` integration
- no usefulness proof
- no policy-improvement proof
- no corpus-wide replay ingestion
- no replay rollout or physics work
- no async/background system
- no database work
- no deterministic-family reopen
- no execution-result cleanup boundary change

## K. RELATION TO NEXT STAGES

### What this pass now guarantees

This pass now guarantees:

- the replay-source materialization reopen decision is explicit and auditable
- the decision stays low-boost-recovery-specific
- the decision stays receipt-bound
- the narrowest honest next contract shape is fixed:
  `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceMaterializationContractV1`
- `source_replay` remains opaque lineage only
- `source_replay.provenance_label` remains opaque lineage only
- `audited_family_root_directory` remains only a BC specimen-tree anchor
- `mimir_export` remains untouched and forbidden

### What remains deferred

This pass still does not guarantee:

- replay source materialization succeeds
- replay file path, replay bytes, or replay frames exist
- actual `mimir_replay::ReplayInput` exists
- replay parsing
- raw-state payload materialization
- tensor/control materialization
- sidecar/manifest necessity

### Immediate next-stage implication

The immediate next pass should be:

- one first narrow contract-definition pass for
  `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplaySourceMaterializationContractV1`
- still above
  `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorActualImplementationRealizationResultV1`
- still without replay-input locator logic
- still without replay parsing
- still without raw-state payload materialization
- still without `mimir_export` widening unless that separate boundary is explicitly reopened
