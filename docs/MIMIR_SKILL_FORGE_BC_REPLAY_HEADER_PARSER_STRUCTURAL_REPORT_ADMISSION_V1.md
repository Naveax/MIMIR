# MIMIR Skill Forge BC Replay Header Parser Structural Report Admission v1

Pass date: 2026-05-02

## A. Purpose

This pass admits, partially admits, or rejects the generated structural report for the already
admitted private-local Rocket League replay fixture:

- fixture id: `rl_replay_header_fixture_001`
- structural report path:
  `artifacts/replay_header_reports/rl_replay_header_fixture_001_structural_report.txt`

This is an evidence-admission pass only. It does not implement MIMIR parser code, does not
implement parser-success logic, does not produce or synthesize a `ReplayHeader`, and does not parse
raw-state payloads, replay frames, or semantic replay events.

The future parser target remains exactly:

- `ReplayReader::read_header(&ReplayInput) -> Result<ReplayHeader>`

The first admitted parser input remains limited to:

- `ReplayInput::Memory { label: "rl_replay_header_fixture_001", bytes: <fixture bytes> }`

## B. Family Scope

The active evidence chain remains scoped first to:

- `low_boost_recovery`

Rocket League replay header parsing remains only a shared `mimir-replay` capability candidate.
This pass does not create replay-source materialization, carrier discovery, locator logic, corpus
ingestion, runtime CLI behavior, async/background systems, database code, rollout physics, or
export widening.

`mimir_export` widening remains forbidden.

## C. Current Fixture Summary

The fixture identity was reverified before this admission decision.

| Field | Value |
| --- | --- |
| fixture id | `rl_replay_header_fixture_001` |
| path | `D:\RocketLeague bot\MIMIR\external_fixtures\sample_001.replay` |
| byte length | `3001021` |
| SHA-256 | `F33EEFADAE8741DE7AA0CEC6D0BEA0120CDF795D16CAF3953A517B4AC6C6EAEB` |
| admission form | `PRIVATE_LOCAL_PATH_WITH_HASH` |
| first parser input boundary | `ReplayInput::Memory` only |

The fixture path, filename, byte length, provenance, and SHA-256 remain fixture identity and
integrity facts only. They are not parser facts.

## D. Prior Report Summary

The prior external parser/tool report remains:

- `artifacts/replay_header_reports/rl_replay_header_fixture_001_report.txt`

The prior approved report admission pass selected Outcome B. It partially retained external
parser facts such as `header_size: 13200`, `header_crc: 2370383193`, `major_version: 868`,
`minor_version: 32`, `net_version: Some(10)`, `game_type: TAGame.Replay_Soccar_TA`,
`header_property_count: 26`, selected header properties, and selected body/footer diagnostics.

Those facts remain external parser/tool report facts only. They are not MIMIR parser output and
not MIMIR parser-success evidence.

## E. Structural Report Identity

The structural report identity was verified from:

- `artifacts/replay_header_reports/rl_replay_header_fixture_001_structural_report.txt`

Observed report size:

- `13790` bytes

The report explicitly states:

- `report_is_mimir_parser_output: no`
- `report_is_parser_success: no`
- `report_is_parser_implementation: no`
- `report_is_full_byte_layout_admission: no`
- `raw_state_payloads_parsed: no`
- `replay_frames_extracted: no`
- `semantic_events_extracted: no`
- `replay_header_synthesized: no`

The report was generated by a temporary external PowerShell structural scanner outside the MIMIR
source/dependency graph. It inspected cached `boxcars 0.11.1` source and the admitted fixture.

## F. Selected Outcome

Selected outcome:

- Outcome B

Outcome B is selected because the structural report is partially admissible. It provides useful
source-backed and fixture-backed structural evidence, but it does not fully close MIMIR mapping
policy, supported-version policy, metadata policy, or parser error-boundary gates.

Why Outcome A is rejected:

- complete expected `ReplayHeader` evidence is not admitted
- `Id` to `ReplayHeader.replay_id` remains a policy decision, not a parser fact
- `NumFrames` to `ReplayHeader.total_frames` remains a policy decision, not a parser fact
- selected properties are not an admitted MIMIR metadata map
- insufficient, malformed, invalid-property, and unsupported-version boundaries are only external
  source candidates
- supported Rocket League replay version or version-family policy remains unadmitted

Why Outcome C is rejected:

- the report is bounded, transparent about non-claims, and cites concrete cached source files plus
  fixture byte offsets
- the top-level field offsets, primitive encodings, property-table shape, terminator, selected
  property routes, and first body boundary candidates are useful for later parser planning

Why Outcome D is rejected:

- admission is bounded by one fixture, one prior report, one structural report, one cached
  external source version, and explicit non-integration rules

## G. Report Section Classification

| Report section | Classification | Admission result |
| --- | --- | --- |
| report metadata and non-claims | admissible report provenance and limitation evidence | admitted as provenance/non-claims only |
| fixture identity check | admissible fixture-integrity evidence | admitted as fixture identity only, not parser facts |
| source evidence inputs from cached `boxcars` | admissible source-route evidence | admitted as external source evidence, not a MIMIR dependency |
| parser structural path summary | admissible structural evidence | partial admission as source-derived parser-rule candidates |
| top-level `header_size` and `header_crc` offsets | admissible byte-layout evidence | admitted for this fixture and as source-derived layout candidates |
| header data start/end candidate | partial byte-layout evidence | admitted for fixture byte accounting; parser stop policy remains partial |
| major/minor/net version offsets and encodings | admissible byte-layout evidence | admitted for this fixture and as source-derived layout candidates |
| `game_type` offset and string encoding | admissible byte-layout evidence | admitted for this fixture and as a source-derived encoding candidate |
| property dictionary encoding and termination | partial byte-layout evidence | admitted for encountered fixture/header structure; general MIMIR policy remains partial |
| top-level property structural table | admissible fixture byte-accounting evidence | admitted for fixture top-level header properties only |
| `Id` structural path candidate | partial expected-output evidence | partial only; no replay id mapping policy admitted |
| `NumFrames` structural path candidate | partial expected-output evidence | partial only; no total frames mapping policy admitted |
| selected metadata property structural paths | partial expected-output evidence | partial only; no metadata key map admitted |
| first body size/CRC boundary candidate | partial stop-boundary evidence | admitted as a fixture boundary candidate only; no body parse admitted |
| insufficient-byte candidate | partial error-boundary evidence | partial only; no MIMIR error taxonomy admitted |
| malformed-byte candidate | partial error-boundary evidence | partial only; no MIMIR error taxonomy admitted |
| unsupported-format/version candidate | rejected as complete boundary evidence | not proven; no MIMIR supported-version policy admitted |
| admission limits | admissible limitation evidence | admitted as non-claims and remaining gates |

## H. Admitted Structural Evidence

The following facts are admitted with narrow scope.

Top-level header fields:

| Evidence | Admitted scope | Source |
| --- | --- | --- |
| `header_size` offset `0`, byte length `4`, encoding `i32_little_endian`, fixture value `13200`, raw hex `90 33 00 00` | fixture byte layout and source-derived layout candidate | structural report `FIXTURE STRUCTURAL OFFSETS`; `boxcars` parser/core parser source |
| `header_crc` offset `4`, byte length `4`, encoding `u32_little_endian`, fixture value `2370383193`, raw hex `59 2D 49 8D` | fixture byte layout and source-derived layout candidate | structural report `FIXTURE STRUCTURAL OFFSETS`; `boxcars` parser/core parser source |

Header field ordering:

| Evidence | Admitted scope | Source |
| --- | --- | --- |
| header data starts at offset `8` for this fixture | fixture byte accounting | structural report `FIXTURE STRUCTURAL OFFSETS` |
| header data end exclusive candidate is `13208` for this fixture | fixture byte accounting and stop-boundary candidate | structural report `FIXTURE STRUCTURAL OFFSETS` |
| `major_version` offset `8`, value `868`, raw hex `64 03 00 00` | fixture byte layout and source-derived layout candidate | structural report and `boxcars` header/core parser source |
| `minor_version` offset `12`, value `32`, raw hex `20 00 00 00` | fixture byte layout and source-derived layout candidate | structural report and `boxcars` header/core parser source |
| `net_version` offset `16`, value `Some(10)`, raw hex `0A 00 00 00` | fixture byte layout and source-derived conditional layout candidate | structural report and `boxcars` header/core parser source |
| `game_type` offset `20`, encoding `windows1252_parse_text_null_terminated`, prefix value `24`, value `TAGame.Replay_Soccar_TA` | fixture byte layout and source-derived encoding candidate | structural report and `boxcars` header/core parser source |

Property table:

| Evidence | Admitted scope | Source |
| --- | --- | --- |
| top-level header properties start at offset `48` | fixture byte accounting | structural report |
| top-level generated property count is `26` | fixture byte accounting | structural report |
| top-level kind counts are `Array=3`, `Float=3`, `Int=12`, `Name=2`, `QWord=1`, `Str=5` | fixture byte accounting | structural report |
| terminator key offset is `13199`, terminator key total byte length is `9`, terminator end offset is `13208`, and terminator matches header end | fixture byte accounting and header-termination candidate | structural report |
| key encoding is `utf8_parse_str_null_terminated` | source-derived structural candidate; fixture terminator evidence | structural report and `boxcars` core parser source |
| property entry shape is key, kind, `property_size` u32 little-endian, four ignored bytes, then value by kind | source-derived structural candidate | structural report and `boxcars` header/core parser source |
| encountered top-level value encodings for `Str`, `Name`, `Int`, `Float`, `QWord`, and `Array` are reported with byte ranges | fixture top-level byte accounting | structural report top-level property table |

First body boundary candidate:

| Evidence | Admitted scope | Source |
| --- | --- | --- |
| `content_size` candidate offset `13208`, encoding `i32_little_endian`, value `2987805` | fixture stop-boundary candidate only | structural report |
| `content_crc` candidate offset `13212`, encoding `u32_little_endian`, value `2323044833` | fixture stop-boundary candidate only | structural report |
| content data start candidate `13216` | diagnostic boundary candidate only | structural report |

No raw-state payload, replay frame, semantic event, body/footer semantic value, or `ReplayHeader`
output is admitted by these facts.

## I. Partial Structural Evidence

The admitted structural evidence is still partial for MIMIR implementation purposes.

| Target | Partial status |
| --- | --- |
| supported Rocket League replay admission rule | partial; external `boxcars` recognition exists, but MIMIR admission policy is missing |
| supported version/version-family policy | partial; fixture has `major=868`, `minor=32`, `net=10`, but no MIMIR policy admits this family |
| header termination rule | partial; fixture terminator matches header end, but MIMIR termination/error policy is not admitted |
| minimal parser stop boundary | partial; first body fields are located, but MIMIR stop behavior remains policy |
| property dictionary | partial; encountered fixture/header shape is admitted, but complete general property policy is not |
| arrays | partial; top-level array ranges and counts are admitted, but nested array semantics are not expected-output evidence |
| CRC | partial; field layout is admitted, but CRC validation policy is not admitted |

## J. Rejected Diagnostic-Only Facts

The following remain rejected for parser facts or expected `ReplayHeader` output:

| Fact | Classification | Reason |
| --- | --- | --- |
| fixture path and filename | fixture identity only | not a parser layout or locator rule |
| byte length and SHA-256 | fixture integrity only | not replay id, metadata, or parser facts |
| external parser success | diagnostic-only for MIMIR | not MIMIR parser success |
| `boxcars` dependency existence in cache | source evidence only | not a project backend dependency |
| body/footer counts from prior report | diagnostic-only | not minimal header evidence |
| `content_size`, `content_crc`, `content_data_start` values | stop-boundary candidates only | no body parsing or body semantics admitted |
| raw-state, replay-frame, semantic-event absence | report-scope non-claims | does not prove future MIMIR parser behavior |

## K. Outcome B Evidence Table

| Evidence target | Structural report source field/section | Admitted status | Scope | Destination | Remaining blocker |
| --- | --- | --- | --- | --- | --- |
| `header_size` offset | `header_size_offset: 0` | admitted | fixture-only plus general parser rule candidate | parser admission, byte-accounting map | none for this fixture field layout; CRC/size validation policy still separate |
| `header_size` field length | `header_size_byte_length: 4` | admitted | fixture-only plus general parser rule candidate | parser admission, byte-accounting map | none for this fixture field layout |
| `header_size` encoding/endian | `header_size_encoding: i32_little_endian` | admitted | fixture-only plus general parser rule candidate | parser admission, byte-accounting map | none for this fixture field layout |
| `header_crc` offset | `header_crc_offset: 4` | admitted | fixture-only plus general parser rule candidate | parser admission, byte-accounting map | no MIMIR CRC validation policy |
| `header_crc` field length | `header_crc_byte_length: 4` | admitted | fixture-only plus general parser rule candidate | parser admission, byte-accounting map | no MIMIR CRC validation policy |
| `header_crc` encoding/endian | `header_crc_encoding: u32_little_endian` | admitted | fixture-only plus general parser rule candidate | parser admission, byte-accounting map | no MIMIR CRC validation policy |
| header data start | `header_data_start_offset: 8` | admitted | fixture-only | parser admission, byte-accounting map | general MIMIR policy still pending |
| header data end | `header_data_end_exclusive_candidate: 13208` | admitted | fixture-only | parser admission, byte-accounting map | termination/error policy still pending |
| first body size/CRC candidate | `content_size_offset_candidate`, `content_crc_offset_candidate` | partial | fixture-only | byte-accounting map | stop-only policy not admitted as parser behavior |
| minimal header parser stop boundary | terminator end `13208`; content size candidate `13208` | partial | fixture-only | parser admission | MIMIR stop/error policy remains missing |
| major version path | `major_version_offset: 8` | admitted | fixture-only plus general parser rule candidate | byte-accounting map | supported-version policy missing |
| minor version path | `minor_version_offset: 12` | admitted | fixture-only plus general parser rule candidate | byte-accounting map | supported-version policy missing |
| net version path | `net_version_offset: 16` | admitted | fixture-only plus general parser rule candidate | byte-accounting map | supported-version policy missing |
| numeric endianness | little-endian source-derived paths | admitted | general parser rule candidate | parser admission, byte-accounting map | MIMIR integer range/error policy missing |
| game type path | `game_type_offset: 20` | admitted | fixture-only plus general parser rule candidate | byte-accounting map | game-type support policy missing |
| game type string encoding | `windows1252_parse_text_null_terminated` | admitted | fixture-only plus general parser rule candidate | byte-accounting map | MIMIR malformed-text policy missing |
| supported version/version-family policy | fixture version fields | partial | fixture-only | parser admission | no MIMIR supported/unsupported rule admitted |
| property table start | `header_properties_start_offset: 48` | admitted | fixture-only | byte-accounting map | none for fixture start offset |
| property entry shape | source-derived property parser summary | admitted | general parser rule candidate | parser admission, byte-accounting map | MIMIR validation policy still missing |
| property key encoding | key `parse_str`; terminator key encoding | admitted | general parser rule candidate | byte-accounting map | malformed key policy missing |
| property terminator | `header_terminator_key_offset`, `header_terminator_end_offset` | admitted | fixture-only | byte-accounting map | general termination error policy missing |
| property kind encoding | kind `parse_str`; table kind prefix offsets | admitted | fixture-only plus general parser rule candidate | byte-accounting map | unsupported kind MIMIR taxonomy missing |
| property size field | `property_size_offset:value` entries; source `u32 LE` | admitted | fixture-only plus general parser rule candidate | byte-accounting map | no MIMIR trust/validation policy for size |
| ignored 4-byte field | `ignored4_offset:hex` entries; source ignored bytes | admitted | fixture-only plus general parser rule candidate | byte-accounting map | no MIMIR validation policy for nonzero values |
| `Str` value encoding | `ReplayName`, `Id`, `MatchGuid`, `Date`, `BuildVersion` rows | admitted | fixture-only plus general parser rule candidate | byte-accounting map | metadata/replay-id policy missing |
| `Name` value encoding | `MapName`, `MatchType` rows | admitted | fixture-only plus general parser rule candidate | byte-accounting map | metadata policy missing |
| `Int` value encoding | `Int` rows, four-byte LE values | admitted | fixture-only plus general parser rule candidate | byte-accounting map | range/mapping policy missing |
| `Float` value encoding | `Float` rows, four-byte LE values | admitted | fixture-only plus general parser rule candidate | byte-accounting map | finite/range/mapping policy missing |
| `QWord` value encoding | `MatchStartEpoch` row, eight-byte LE value | admitted | fixture-only plus general parser rule candidate | byte-accounting map | metadata policy missing |
| `Array` value encoding | `Goals`, `HighLights`, `PlayerStats` rows | partial | fixture-only | byte-accounting map | nested semantics and expected-output mapping not admitted |
| top-level property count | `header_property_count_top_level_generated: 26` | admitted | fixture-only | byte-accounting map | none for fixture count |
| `Id` structural path | selected candidate `header.properties[20]` | partial | fixture-only | `ReplayHeader.replay_id` | replay id mapping/validation policy missing |
| future memory label to source label | admitted input contract, not structural report | admitted | fixture-only input contract | `ReplayHeader.source_label` | only after future parser success; not parser-success evidence |
| `NumFrames` structural path | selected candidate `header.properties[24]` | partial | fixture-only | `ReplayHeader.total_frames` | total frames mapping/range/`None` policy missing |
| selected metadata property paths | selected candidate property routes | partial | fixture-only | `ReplayHeader.metadata` | metadata key/value map or explicit empty policy missing |
| insufficient-byte boundary | `SOURCE-DERIVED ERROR/BOUNDARY CANDIDATES` | partial | general parser rule candidate | error taxonomy | MIMIR boundary cases and errors not admitted |
| malformed-byte boundary | text too large, unknown kind, oversized list candidates | partial | general parser rule candidate | error taxonomy | MIMIR malformed taxonomy not admitted |
| unsupported-format/version boundary | report says not proven | rejected as complete evidence | diagnostic-only | error taxonomy | supported version/version-family policy missing |
| invalid field/property boundary | unknown kind and size/list candidates | partial | general parser rule candidate | error taxonomy | MIMIR invalid-property policy missing |

## L. Byte-Accounting Map Status

Byte-accounting status:

- partial

The report now admits a fixture-only byte-accounting map for the top-level header prefix, version
fields, game type, top-level property table, selected property value ranges, terminator, and first
body boundary candidates.

The byte-accounting map remains incomplete for implementation because:

- it is admitted first as fixture evidence, not corpus-wide replay ingestion evidence
- supported version/version-family policy is missing
- complete MIMIR property validation policy is missing
- CRC validation behavior is not admitted
- parser stop/error behavior is not admitted
- nested array semantics are not expected-output evidence

## M. Expected ReplayHeader Evidence Status

Expected `ReplayHeader` evidence status:

- partial

No complete `ReplayHeader` is admitted.

| `ReplayHeader` field | Status | Reason |
| --- | --- | --- |
| `replay_id` | partial | `Id` has a structural path and value candidate, but no MIMIR mapping or validation policy |
| `source_label` | admitted only from future `ReplayInput::Memory.label` | source label is an input contract fact, not report output or parser success |
| `total_frames` | partial | `NumFrames` has a structural path and value candidate, but no MIMIR mapping/range/`None` policy |
| `metadata` | partial | selected properties have structural paths, but no metadata key map or empty metadata policy |

## N. Replay Id Mapping Status

Replay id mapping status:

- partial

Admitted structural candidate:

- key: `Id`
- structural path: `header.properties[20]`
- key offset: `12849`
- kind: `Str`
- value range: `[12880,12917)`
- value encoding: `windows1252_parse_text_null_terminated`
- generated value: `7F59297811EFD8B19C444A81FB07660C`

Blocked policy:

- no MIMIR policy admits direct `Id` to `ReplayHeader.replay_id`
- no validation rule admits that value shape as a `ReplayId`
- replay id must not be derived from fixture id, path, filename, provenance, byte length, or
  SHA-256

## O. Source Label Mapping Status

Source label mapping status:

- admitted only from the future `ReplayInput::Memory` input contract

Expected future input label:

- `rl_replay_header_fixture_001`

This is not byte-layout evidence, not structural report output, and not parser-success evidence.

## P. Total Frames Mapping Status

Total frames mapping status:

- partial

Admitted structural candidate:

- key: `NumFrames`
- structural path: `header.properties[24]`
- key offset: `13107`
- kind: `Int`
- value range: `[13145,13149)`
- value encoding: `i32_little_endian_4_bytes`
- generated value: `13555`

Blocked policy:

- no MIMIR policy admits direct `NumFrames` to `ReplayHeader.total_frames`
- no range or conversion policy from signed `i32` to `Option<u32>` is admitted
- no explicit `None` policy is admitted

## Q. Metadata Mapping Status

Metadata mapping status:

- partial

Selected metadata candidates with structural paths include `ReplayName`, `Date`, `MapName`,
`ReplayVersion`, `BuildVersion`, `MaxChannels`, `MatchType`, `TeamSize`, and `RecordFPS`.

Blocked policy:

- no admitted metadata key map
- no admitted value type mapping to `mimir_types::FieldValue`
- no admitted omission/default policy
- no explicit empty metadata policy
- no malformed metadata behavior

## R. Error-Boundary Evidence Status

Error-boundary evidence status:

- partial

External source-derived candidates exist for:

- insufficient data through `CoreParser` byte access failures
- malformed text through `TextTooLarge`
- malformed or unsupported property kind through `UnexpectedProperty`
- oversized list/array through `ListTooLarge`
- CRC mismatch/corrupt replay candidates

These are not yet MIMIR parser error boundaries. Missing MIMIR boundaries include:

- insufficient bytes for top-level prefix, header data, strings, properties, and selected values
- malformed bytes for string encodings and invalid property shapes
- invalid field/property policy
- CRC validation policy
- unsupported format/version policy, if distinguishable
- expected `ReplayHeader` mapping failure policy

## S. Parser Implementation Reopen Status

Parser implementation remains closed.

Parser-success logic remains closed.

Implementation may not proceed from this pass because:

1. complete expected `ReplayHeader` evidence is not admitted
2. replay id mapping remains partial
3. total frames mapping remains partial
4. metadata mapping remains partial
5. supported version/version-family policy remains missing
6. MIMIR error boundaries remain partial or missing
7. implementation has not been explicitly reopened

## T. No-Fake-Evidence Rules

This pass admits no invented:

- magic bytes
- offsets
- field lengths
- endianness
- version layout
- header boundary outside the report-backed fixture accounting
- replay id derivation
- total frame derivation
- metadata key map
- parser-success fixture output
- insufficient-byte fixture
- malformed-byte fixture
- unsupported-format fixture

The structural report is not MIMIR parser output.

External tool success is not MIMIR parser success.

## U. What Remains Closed

Still closed after this pass:

- parser implementation
- parser-success logic
- `ReplayHeader` production or synthesis
- raw-state payload parsing
- replay frame extraction
- semantic replay event extraction
- replay-source actual materialization
- replay-source carrier discovery
- replay-input locator logic
- corpus-wide replay ingestion
- runtime CLI commands
- async/background systems
- database code
- real rollout physics
- `mimir_export` widening

## V. What Remains Forbidden

Still forbidden unless explicitly reopened:

- modifying `mimir-io`
- modifying `mimir-export`
- modifying `mimir-types`
- modifying `Cargo.toml`
- modifying `Cargo.lock`
- adding project backend dependencies
- implementing parser code
- implementing parser-success logic
- producing or synthesizing `ReplayHeader`
- parsing raw-state payloads
- extracting replay frames
- extracting semantic replay events
- implementing replay-source actual materialization
- implementing replay-source carrier discovery
- implementing replay-input locator logic
- widening export semantics
- adding corpus-wide replay ingestion
- adding runtime CLI commands
- adding async/background systems
- adding database code
- adding real rollout physics
- treating fixture path, filename, provenance, byte length, or SHA-256 as parser facts

## W. Next Stage

Immediate next pass:

- ReplayHeader mapping policy admission for `rl_replay_header_fixture_001`

The next pass must decide whether to admit, block, or explicitly defer:

- `Id` -> `ReplayHeader.replay_id`
- `ReplayInput::Memory.label` -> `ReplayHeader.source_label`
- `NumFrames` -> `ReplayHeader.total_frames`
- selected property keys and values -> `ReplayHeader.metadata`
- malformed or missing selected mapping behavior

Parser implementation is still not the next pass. Parser implementation is allowed only after
fixture evidence, complete byte-layout evidence, complete expected `ReplayHeader` evidence, and
required error boundaries are admitted, and implementation is explicitly reopened.
