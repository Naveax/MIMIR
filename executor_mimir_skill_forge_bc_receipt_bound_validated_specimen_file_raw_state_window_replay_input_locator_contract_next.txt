executor mimir skill forge bc receipt-bound validated specimen-file raw-state-window replay-input locator contract next step
repo root: D:\RocketLeague bot\MIMIR
date: 2026-04-16

next pass
- a first actual replay-input locator realization pass above
  `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorContractV1`,
  still without replay-input locator implementation
- `mimir_export` widening remains forbidden unless explicitly reopened

classification of the next pass
- a first actual replay-input locator realization pass still without locator implementation

why this is the immediate next pass
- The truthful non-located replay-input-access/source-binding realization result is already
  explicit.
- The replay-input locator reopen decision is already explicit.
- This pass now makes the replay-input locator contract explicit in prose and code.
- The remaining unresolved problem is no longer whether locator work should reopen or what the
  contract shape is.
- The remaining unresolved problem is whether the contract can now produce a first truthful
  locator-realization result without pretending locator implementation exists.

what the next pass should consume
- `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorContractV1`
- the preserved audited family root directory reference already carried by that contract
- nothing from `mimir_export` unless a separate explicit reopen says otherwise

what the next pass should do
- remain strictly low-boost-recovery-specific
- remain strictly receipt-bound
- realize the contract-defined replay-input locator boundary without implementing actual replay
  locator logic
- preserve the invariant that `source_replay` remains opaque lineage
- preserve the invariant that the audited family root remains only a BC specimen-tree anchor
- preserve the invariant that deferred observation lineage stays deferred
- preserve that `FutureParserConsumableReplayHandleOnly` is still not locator success
- stay below actual replay parsing
- stay below actual raw-state payload, tensor, and control materialization

what the next pass must still not do
- `mimir_export` widening remains forbidden unless explicitly reopened
- no actual replay-input locator implementation
- no actual replay parsing implementation
- no actual raw-state payload materialization
- no tensor materialization
- no control/action extraction
- no sidecar/manifest realization unless a separate defect-driven reopen proves one is required
- no generic multi-family replay/raw-state/index/export/materialization framework
- no generic manifest/index framework
- no rollout/physics/runtime work

why the next pass is not another contract refinement pass
- This pass already froze the narrowest honest contract surface.
- Another refinement pass would only restate the same contract instead of resolving the next real
  missing step.
- The remaining honest work is realization of the contract-defined replay-input locator boundary,
  not more contract wording.

why the next pass is not a broader reopen decision
- The still-missing work is already localized to one replay-input locator realization boundary.
- Broadening the reopen question would widen scope without evidence.
