executor mimir skill forge bc receipt-bound validated specimen-file raw-state-window replay-input locator actual-implementation contract decision
repo root: D:\RocketLeague bot\MIMIR
date: 2026-04-17

exact contract-definition role chosen
- one low-boost-recovery-specific contract-definition boundary above:
  - `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorImplementationRealizationResultV1`
- exact job:
  - admit only the existing truthful blocked replay-input locator implementation realization result
  - revalidate the exact receipt-bound root/order/path and per-specimen identity tuple already
    frozen by that realization
  - define one replay-input locator actual-implementation-facing contract-only handle boundary per
    admitted specimen
  - preserve the current truth that actual replay-input locator implementation, locator success,
    parsing success, and payload materialization are still deferred
- actual-implementation reopen-decision docs/artifacts were used as audit guidance only:
  - they fixed the chosen next shape
  - they were not turned into a new typed Rust input boundary in this pass

exact contract shape defined
- exact contract-shape type:
  - `LowBoostRecoveryBcReceiptBoundReplayInputLocatorActualImplementationContractShapeV1`
- exact contract disposition type:
  - `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorActualImplementationContractDispositionV1`
- exact contract note type:
  - `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorActualImplementationContractNoteV1`
- exact actual-implementation-facing boundary types:
  - `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorActualImplementationBoundaryInputV1`
  - `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorActualImplementationOutputBoundaryV1`
- exact lane/specimen result types:
  - `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorActualImplementationContractLaneResultV1`
  - `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorActualImplementationContractSpecimenResultV1`
- exact top-level result type:
  - `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorActualImplementationContractV1`
- exact error type:
  - `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorActualImplementationContractDefinitionError`
- exact entry function:
  - `define_low_boost_recovery_bc_receipt_bound_validated_specimen_file_raw_state_window_replay_input_locator_actual_implementation_contract_v1(...)`
- exact per-specimen admitted fields:
  - `admitted_replay_input_locator_implementation_boundary_input`
  - `admitted_replay_input_locator_implementation_output_boundary`
  - `admitted_replay_input_locator_implementation_disposition`
- exact per-specimen actual-implementation-facing fields:
  - `replay_input_locator_actual_implementation_boundary_input`
  - `replay_input_locator_actual_implementation_output_boundary`

exact output / disposition chosen
- exact disposition:
  - `ContractDefinedForReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputLocatorActualImplementationOnly`
- exact chosen shape:
  - `ReceiptBoundReplayInputLocatorActualImplementationOnly`
- exact actual-implementation-facing output-boundary rule:
  - `bound_replay_input_locator_handle_kind` remains
    `FutureParserConsumableReplayHandleOnly`
- exact contract-note commitments:
  - preserve the replay-input locator implementation realization boundary
  - revalidate the truthful blocked implementation input without reopening lower layers
  - preserve the audited family root only as a BC specimen-tree anchor
  - preserve lane/specimen order, artifact ids, anchored specimen-file paths, source raw-state
    window refs, and deferred observation lineage
  - preserve `source_replay` as opaque lineage only
  - preserve `FutureParserConsumableReplayHandleOnly` as contract-only and not locator success
  - allow future `mimir_replay::ReplayInput` targeting to be acknowledged only as deferred intent,
    without materialization or promise
  - keep actual replay-input locator implementation, replay parsing, raw-state payload
    materialization, tensor/control materialization, sidecar/manifest/index work, and
    `mimir_export` integration deferred

exact admission / failure decisions chosen
- Admit only the exact replay-input locator implementation realization result already inside the
  low-boost-recovery receipt-bound chain.
- Require the exact replay-input locator implementation contract disposition/note set, the exact
  replay-input locator implementation realization disposition/note set, the exact chosen shapes,
  the exact audited root reference, and exact lane/specimen order.
- Require every admitted realization specimen to remain identical on:
  - `lane_ordinal`
  - `specimen_ordinal`
  - `artifact_id`
  - `anchored_bc_specimen_file_path`
  - `source_raw_state_window_ref`
  - `source_slice_id`
  - `source_replay`
  - `source_subject`
  - `source_phase_id`
  - `preserved_observation_binding_kind`
  - `preserved_accepted_reference_window`
- Require the preserved locator handle kind to remain
  `FutureParserConsumableReplayHandleOnly`.
- Hard fail on degraded realization input.
- Hard fail on mismatched counts, root, order, or artifact ids.
- Hard fail on any attempt to reinterpret `source_replay`, `provenance_label`, or the audited
  family root as an implicit replay locator or replay storage.
- Hard fail on any attempt to widen this pass into actual replay-input locator implementation,
  replay parsing, raw-state payload materialization, tensor/control materialization, sidecars,
  manifests, generic indexing, or `mimir_export`.

what was intentionally excluded
- any actual replay-input locator implementation
- any actual replay parsing implementation
- any actual raw-state payload materialization
- any tensor materialization
- any control/action extraction
- any `mimir_export` integration
- any `mimir_export` surface edits
- any sidecar/manifest realization
- any generic multi-family replay/raw-state/index/export/materialization framework
- any generic manifest/index framework
- any usefulness-proof or policy-improvement claim

whether Rust/code changes were added, and why or why not
- Yes.
- Added in `crates/mimir-skill/src/lib.rs` only:
  - the replay-input locator actual-implementation contract DTOs
  - the replay-input locator actual-implementation contract-definition error surface
  - strict validation of the blocked implementation realization input for this contract boundary
  - `define_low_boost_recovery_bc_receipt_bound_validated_specimen_file_raw_state_window_replay_input_locator_actual_implementation_contract_v1(...)`
  - narrow tests covering valid semantics, degraded realization rejection, identity-drift
    rejection, and determinism
- No `mimir-types`, `mimir-replay`, `mimir-io`, or `mimir-export` changes were added.
- Reason code changes were justified:
  - prior family contract-definition boundaries in this repo are machine-verifiable `mimir-skill`
    surfaces
  - leaving this pass as prose only would force the next pass to guess the exact admitted
    actual-implementation-facing contract shape
  - the contract stays narrow without reopening replay ownership or changing `mimir_export`

why this is enough for the next pass
- The repo now has one explicit machine-verifiable replay-input locator actual-implementation
  contract boundary above the truthful blocked replay-input locator implementation realization
  result.
- That contract stays strictly low-boost-recovery-specific and strictly receipt-bound.
- The exact admitted specimen evidence and exact actual-implementation-facing boundary input/output
  shape are now frozen instead of implied.
- The next immediate question is therefore no longer what the minimum actual-implementation-facing
  contract must be.
- The next immediate question is the first narrow actual replay-input locator implementation pass
  above this contract.
- `mimir_export` widening remains unnecessary and still forbidden unless explicitly reopened.
