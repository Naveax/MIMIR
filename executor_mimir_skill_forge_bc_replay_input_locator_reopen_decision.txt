executor mimir skill forge bc replay-input locator reopen decision
repo root: D:\RocketLeague bot\MIMIR
date: 2026-04-16

exact reopen role chosen
- one low-boost-recovery-specific reopen-decision boundary above:
  - `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputAccessSourceBindingRealizationResultV1`
- exact job:
  - admit only the existing truthful non-located replay-input-access/source-binding realization
    result
  - revalidate that the admitted realization still preserves the exact receipt-bound root/order/path
    and per-specimen identity tuple
  - decide whether replay-input locator work must now be deliberately reopened
  - if reopened, fix only the minimum honest next contract shape
  - stay strictly below replay-input locator implementation, replay parsing, and
    raw-state/tensor/control materialization

exact reopen / no-reopen decision chosen
- exact decision:
  - reopen justified for one narrow receipt-bound replay-input locator boundary
- no separate valid-input no-reopen disposition was added
- reason:
  - for valid admitted input, the current repo already proves the remaining missing piece is one
    replay-input locator boundary above the truthful non-located replay-input-access/source-binding
    result

exact criteria used
- The remaining missing piece had to be replay-input locator work, not another lower-layer defect.
- The next boundary had to remain low-boost-recovery-specific.
- The next boundary had to remain strictly receipt-bound.
- The next boundary had to avoid sidecars, manifests, and generic indexing.
- The next boundary had to avoid `mimir_export`.
- The next boundary had to remain below actual replay parsing.
- The next boundary had to remain below actual raw-state payload, tensor, and control
  materialization.
- The next boundary had to introduce only one locator-facing handle contract without inventing
  replay-path semantics absent from the admitted realization result.
- The repo audit confirmed all of the above and also confirmed that `mimir-replay` still exposes
  only `ReplayInput`, `ReplayHeader`, `ReplayReader`, and `UnsupportedReplayReader`.

exact output / disposition chosen
- exact top-level decision type:
  - `LowBoostRecoveryBcReceiptBoundReplayInputLocatorReopenDecisionResultV1`
- exact disposition type:
  - `LowBoostRecoveryBcReceiptBoundReplayInputLocatorReopenDispositionV1`
- exact note type:
  - `LowBoostRecoveryBcReceiptBoundReplayInputLocatorReopenNoteV1`
- exact chosen contract-shape type:
  - `LowBoostRecoveryBcReceiptBoundReplayInputLocatorContractShapeV1`
- exact entry function:
  - `decide_low_boost_recovery_bc_receipt_bound_replay_input_locator_reopen_from_receipt_bound_validated_specimen_file_raw_state_window_replay_input_access_source_binding_realization_v1(...)`
- exact disposition:
  - `ReopenJustifiedForReceiptBoundReplayInputLocatorContract`
- exact chosen shape:
  - `ReceiptBoundReplayInputLocatorOnly`

exact admission / failure decisions chosen
- Admit only the exact truthful non-located replay-input-access/source-binding realization result.
- Require the exact replay-input-access/source-binding contract disposition/note set, the exact
  truthful non-located realization disposition/note set, the exact locator/materialization/replay-
  parsing shapes, the exact chosen replay-input-access contract shape, the exact audited family
  root basename/existence/directory-ness, and exact lane/specimen order.
- Require every admitted boundary input/output pair to remain identical across:
  - `lane_ordinal`
  - `specimen_ordinal`
  - `artifact_id`
  - `anchored_bc_specimen_file_path`
  - `source_raw_state_window_ref`
  - `source_slice_id`
  - `source_replay`
  - `source_subject`
  - `source_phase_id`
  - `preserved_observation_binding_kind`
  - `preserved_accepted_reference_window`
- Require
  `bound_replay_input_access_source_binding_handle_kind == FutureParserConsumableReplayHandleOnly`.
- Require every specimen disposition to remain
  `RealizedForTruthfulNonLocatedReplayInputAccessSourceBindingOnly`.
- Hard fail on degraded realization input.
- Hard fail on count/order/root/path drift.
- Hard fail on artifact-id drift or duplicate artifact ids.
- Hard fail on any attempt to reinterpret `source_replay`, `provenance_label`, or the audited
  family root as an implicit replay locator.
- Hard fail on any attempt to widen this pass into replay-input locator implementation, replay
  parsing, raw-state payload materialization, tensor/control materialization, sidecars, manifests,
  generic indexing, or `mimir_export`.

what was intentionally excluded
- any actual replay-input locator implementation
- any actual replay parsing implementation
- any actual raw-state payload materialization
- any tensor materialization
- any control/action extraction
- any `mimir_export` integration
- any `mimir_export` surface edits
- any sidecar/manifest realization
- any generic multi-family replay/raw-state/index/export/materialization framework
- any generic manifest/index framework
- any usefulness-proof or policy-improvement claim

whether Rust/code changes were added, and why or why not
- Yes.
- Added in `crates/mimir-skill/src/lib.rs` only:
  - the replay-input locator reopen-decision DTOs
  - the reopen-decision error surface
  - strict realization validation for this boundary
  - `decide_low_boost_recovery_bc_receipt_bound_replay_input_locator_reopen_from_receipt_bound_validated_specimen_file_raw_state_window_replay_input_access_source_binding_realization_v1(...)`
  - narrow tests covering valid semantics, degraded realization rejection, identity-drift
    rejection, and determinism
- Added because prior family reopen decisions in this repo are machine-verifiable `mimir-skill`
  layers, and the next contract-definition pass should consume one explicit decision result instead
  of re-inferring reopen semantics from prose.
- No `mimir-types`, `mimir-replay`, `mimir-io`, or `mimir-export` changes were added.

why this is enough for the next pass
- The repo now has one explicit machine-verifiable replay-input locator reopen decision above the
  truthful non-located replay-input-access/source-binding result.
- That decision stays strictly low-boost-recovery-specific and strictly receipt-bound.
- The next pass no longer needs to guess whether locator work is still closed or what the minimum
  honest next boundary shape is.
- The next immediate task is now obvious and narrow:
  - define the first replay-input locator contract above
    `LowBoostRecoveryBcReceiptBoundValidatedSpecimenFileRawStateWindowReplayInputAccessSourceBindingRealizationResultV1`
- `mimir_export` widening remains unnecessary and still forbidden unless explicitly reopened.
