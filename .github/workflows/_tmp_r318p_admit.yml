name: R3.18P Clean Contract Admission

on:
  push:
    branches: [ops/r318p-contract-prepare]

permissions:
  contents: write
  actions: read

jobs:
  prepare:
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    env:
      RUSTUP_TOOLCHAIN: 1.85.0
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v6
        with: {fetch-depth: 0}
      - name: Toolchain
        run: |
          set -euo pipefail
          rustup toolchain install 1.85.0 --profile minimal --component rustfmt --component clippy
          rustc --version | grep -F 'rustc 1.85.0 '
      - name: Build exact contract and clean admission commit
        env:
          BASE_MAIN: b2d9feaee5d7579c88466431dd52d879cddfb70b
          ADMISSION_BRANCH: admission/r318p-header-context-contract
        run: |
          set -euxo pipefail
          OPS="$PWD"; ART="$(mktemp -d)"; WORK="$(mktemp -d)"
          trap 'rm -rf "$ART" "$WORK"' EXIT
          git fetch origin main --force
          test "$(git rev-parse origin/main)" = "$BASE_MAIN"

          gh run download 32017369100 -n r318o-following-property-header-evidence -D "$ART"
          test "$(sha256sum "$ART/r3_18o_source_summary.json" | awk '{print $1}')" = a261368f51770efee56e3d8d760390f633b6190bed81446feaf57b076189ae01
          test "$(sha256sum "$ART/r3_18o_header_rows.json" | awk '{print $1}')" = 503bae96ac51ff27532fc80b5e537b3cb7ccd58cea1584a9a1f975da8a4748a9
          test "$(sha256sum "$ART/r3_18o_aggregate.txt" | awk '{print $1}')" = 02324f5a0caa68257a0af93999245124242569f8d582ab2aba2f8119fe6cd676
          (cd "$ART" && sha256sum -c r3_18o_artifact_sha256.txt)
          grep -Fq 'R3_18O_OUTCOME=A' "$ART/r3_18o_aggregate.txt"
          grep -Fq 'R3_18O_FROZEN_ROWS=47/47' "$ART/r3_18o_aggregate.txt"
          grep -Fq 'R3_18O_WITNESS_RESELECTION=0' "$ART/r3_18o_aggregate.txt"
          grep -Fq 'R3_18O_FOLLOWING_HEADER_EXACT=47/47' "$ART/r3_18o_aggregate.txt"
          grep -Fq 'R3_18O_NATIVE_ORACLE_MISMATCH=0' "$ART/r3_18o_aggregate.txt"
          echo 'R3_18P_O_AUTHORITY=PASS'

          git worktree add --detach "$WORK" "$BASE_MAIN"
          python3 "$OPS/tools/_tmp_r318p_admit.py" "$WORK" "$ART/r3_18o_source_summary.json"
          cd "$WORK"
          git diff --check
          expected=(
            MIMIR_CONTINUE_HERE.md MIMIR_KNOWLEDGE_GRAPH.md
            docs/continuity/MIMIR_BOUNDARY_LOCKS.md docs/continuity/MIMIR_CONTINUITY_STATE.json
            docs/continuity/MIMIR_CURRENT_STATE.md docs/continuity/MIMIR_NEXT_CHAT_HANDOFF.md
            docs/continuity/MIMIR_PROGRESS_LEDGER.md docs/continuity/MIMIR_R3_18P_ADMITTED_HEADER_CONTEXTS.json
            docs/continuity/MIMIR_R3_18P_DECISION.md docs/continuity/MIMIR_R3_18Q_EXECUTION_SPEC.md
          )
          mapfile -t changed < <(git status --short | sed -E 's/^.. //' | sort)
          mapfile -t want < <(printf '%s\n' "${expected[@]}" | sort)
          printf '%s\n' '--- CHANGED ---' "${changed[@]}"
          test "${#changed[@]}" -eq 10
          test "$(printf '%s\n' "${changed[@]}")" = "$(printf '%s\n' "${want[@]}")"
          git diff --exit-code "$BASE_MAIN" -- crates Cargo.toml Cargo.lock external_fixtures test_corpus scripts .github/workflows

          python3 - <<'PY'
          import json,hashlib
          from pathlib import Path
          c=Path('docs/continuity/MIMIR_R3_18P_ADMITTED_HEADER_CONTEXTS.json')
          d=json.loads(c.read_text())
          assert d['observed_row_count']==47 and d['unique_exact_context_count']==18
          assert len(d['admitted_contexts'])==18 and sum(x['observed_count'] for x in d['admitted_contexts'])==47
          keys=('stream_id_bound','prop_id_bits','property_object_index','attribute_tag','version_major','version_minor','net_version')
          exact={tuple(x[k] for k in keys) for x in d['admitted_contexts']}
          assert len(exact)==18
          # Mandatory anti-widening negatives.
          sample=next(iter(exact)); assert (sample[0],sample[1],sample[2],'__OUTSIDE__',sample[4],sample[5],sample[6]) not in exact
          assert (sample[0],sample[1],sample[2],sample[3],0,0,0) not in exact
          assert (999,9,999,'Boolean',868,32,10) not in exact
          assert d['membership_policy']=='exact_tuple_only'
          h=hashlib.sha256(c.read_bytes()).hexdigest()
          assert h in Path('docs/continuity/MIMIR_R3_18P_DECISION.md').read_text()
          st=json.loads(Path('docs/continuity/MIMIR_CONTINUITY_STATE.json').read_text())
          assert st['current_pass']=='R3.18Q' and st['last_completed_contract_pass']=='R3.18P'
          f=st['next_files_to_read']; trio=['docs/continuity/MIMIR_R3_18P_ADMITTED_HEADER_CONTEXTS.json','docs/continuity/MIMIR_R3_18P_DECISION.md','docs/continuity/MIMIR_R3_18Q_EXECUTION_SPEC.md']
          p=f.index('docs/continuity/MIMIR_R3_18P_EXECUTION_SPEC.md'); assert f[p+1:p+4]==trio
          print('R3_18P_CONTRACT_VALIDATION=PASS',h)
          PY
          grep -Fq 'R3.18P following-property header context contract: OUTCOME A / CLOSED' MIMIR_KNOWLEDGE_GRAPH.md
          grep -Fq 'R3.18Q bounded following-property header composition: ACTIVE' MIMIR_KNOWLEDGE_GRAPH.md
          grep -Fq '# CURRENT PASS CHECKLIST — R3.18Q' MIMIR_CONTINUE_HERE.md
          pwsh -NoProfile -File scripts/verify_repo.ps1

          git config user.name 'MIMIR Admission Bot'; git config user.email 'actions@users.noreply.github.com'
          git add -- "${expected[@]}"; git commit -m 'Admit R3.18P and open R3.18Q'
          SHA="$(git rev-parse HEAD)"; TREE="$(git rev-parse HEAD^{tree})"
          test "$(git rev-parse HEAD^)" = "$BASE_MAIN"
          test "$(git diff --name-only "$BASE_MAIN" HEAD | sort)" = "$(printf '%s\n' "${want[@]}")"
          git diff --exit-code "$BASE_MAIN" HEAD -- crates Cargo.toml Cargo.lock external_fixtures test_corpus scripts .github/workflows
          pwsh -NoProfile -File scripts/verify_repo.ps1
          if git ls-remote --exit-code --heads origin "$ADMISSION_BRANCH" >/dev/null 2>&1; then exit 1; fi
          git push origin "HEAD:refs/heads/$ADMISSION_BRANCH"
          test "$(git ls-remote origin "refs/heads/$ADMISSION_BRANCH" | awk '{print $1}')" = "$SHA"
          printf 'R3_18P_CLEAN_SHA=%s\nR3_18P_CLEAN_TREE=%s\nR3_18P_PARENT=%s\nR3_18P_SCOPE=10_DOCS_ONLY\n' "$SHA" "$TREE" "$BASE_MAIN" > "$OPS/r3_18p_receipt.txt"
      - uses: actions/upload-artifact@v4
        with:
          name: r318p-clean-admission-receipt
          path: r3_18p_receipt.txt
          if-no-files-found: error
          retention-days: 30
