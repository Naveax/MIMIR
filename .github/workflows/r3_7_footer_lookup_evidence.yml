name: R3.7 Footer Lookup Evidence

on:
  push:
    branches:
      - agent/r3-7-footer-lookup-evidence

jobs:
  evidence:
    runs-on: windows-2025
    steps:
      - uses: actions/checkout@v6
      - name: Decode footer lookup candidates and measure referential integrity
        shell: pwsh
        run: python tools/r3_7_footer_lookup_evidence.py
