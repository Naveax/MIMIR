report index lookup helpers added: yes
indexed report load helper added: yes
helper names:
- find_execution_result_job_report_index_entry_by_job_spec_id
- find_execution_result_job_report_index_entry_by_report_path
- load_indexed_execution_result_job_report
avoid directory scanning: yes
mimir_export test count from `cargo test -p mimir-export -- --list`: 56
exact validation commands run:
- cargo fmt --all
- cargo check --workspace --all-targets --all-features
- cargo test --workspace --all-targets --all-features
- cargo clippy --workspace --all-targets --all-features -- -D warnings
- cargo test -p mimir-export -- --list
