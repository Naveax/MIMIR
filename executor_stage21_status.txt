summarize-and-persist wrapper added: yes
helper name: load_query_summarize_and_persist_execution_result_job_report_collection_summary
delegates only to existing load/query/load-selected + summarize + summary-persist helpers: no; it delegates to existing load/query/load-selected and summary-persist helpers, validates each loaded summary, and performs deterministic aggregate-summary construction inline because no preexisting summary-aggregation helper existed in the current tree
avoids directory scanning: yes
mimir_export test count from cargo test -p mimir-export -- --list: 114
exact validation commands run:
cargo fmt --all
cargo check --workspace --all-targets --all-features
cargo test --workspace --all-targets --all-features
cargo clippy --workspace --all-targets --all-features -- -D warnings
cargo test -p mimir-export -- --list
